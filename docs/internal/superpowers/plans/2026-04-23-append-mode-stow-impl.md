# Append-mode stow implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make `stow(df, "metrics")` append to a single growing table indexed by run (Shape B), while `stow(obj, "thing")` for non-tabular values continues to version (Shape A). Add `prune_runs()` and an optional `run=` arg to `grab()`.

**Architecture:** A new bookkeeping table `_mr_append_tables` tracks the run-indexed accumulators alongside existing `_mr_versions`. A thin namespace module routes each logical name to exactly one shape. `stow()` dispatches on value type → shape; `grab()` dispatches on shape → reader. Schema drift across runs is reconciled losslessly (ALTER TABLE ADD for new columns; NULL-fill for missing; coerce-to-TEXT for type conflicts). Launch-context detection — "is this grab inside an active launch?" — piggybacks on existing `.mr_state$recording`, which is extended to carry the current `run_id` and `variant_label` so Shape B rows can be stamped.

**Scope decision (vs. spec §12):** This plan adds the new shape-specific files (`shape_append.R`, `namespace.R`, `prune.R`) that Shape B genuinely needs, and adds lightweight dispatch branches inside the existing `stow.R` / `grab.R`. The deeper reorg — carving Shape A out of `stow.R` / `grab.R` into a new `shape_versioned.R`, folding `versions.R` — is queued as a follow-up. Rationale: file moves are organizational; bundling them with behavior changes makes review harder and rollback riskier. Since Shape B lives in its own files from day one, the follow-up reorg is a straight lift-and-shift of Shape A.

**Open points flagged in the plan (not blockers):**
- Variant-label stamping is done at stow-time from the user-supplied `label`. If the user didn't pass `label` and propagation later *infers* one, the already-stowed rows keep `NA` while the `_mr_runs` row gets the inferred label. The motivating sweep use case always passes an explicit label, so this is a corner only.
- Shape B reads record `(name, NA)` on `_mr_runs.inputs`. Staleness on Shape B inputs is a no-op in v0.1; upstream-launch staleness still drives invalidation.

**Tech Stack:** R 4.5.x, `DBI`, `duckdb`, `dbplyr`, `testthat (3e)`, `devtools`, `roxygen2`, `jsonlite`, `qs2` (existing Imports only — no new deps, invariant 6).

---

## File structure

### New files (R/)
- `R/namespace.R` — `.mr_lookup_shape()` resolves a logical name to `"A" | "B" | NULL`; `.mr_guard_namespace()` moves here and gains Shape-B awareness.
- `R/shape_append.R` — Shape B internals: `.mr_append_ensure_table()`, `.mr_append_reconcile_schema()`, `.mr_append_write_frame()`, `.mr_append_write_lazy()`, `.mr_append_query()`, `.mr_append_row_hash()`.
- `R/prune.R` — `.mr_prune_runs_rows()` (internal) + new exported `prune_runs()`.

### New files (tests/)
- `tests/testthat/test-append-schema.R`
- `tests/testthat/test-append-stow.R`
- `tests/testthat/test-append-schema-drift.R`
- `tests/testthat/test-append-grab.R`
- `tests/testthat/test-append-rebind.R`
- `tests/testthat/test-append-prune.R`
- `tests/testthat/test-append-integration.R`

### Modified files
- `R/schema.R` — add `_mr_append_tables` migration.
- `R/stow.R` — data.frame / tbl_lazy branches delegate to Shape B.
- `R/grab.R` — dispatch on shape; Shape B reader branch + new `run=` arg.
- `R/recording.R` — extend `.mr_start_recording(run_id, variant_label)`; expose getters.
- `R/launch_one.R` — pass `run_id`, `label` into `.mr_start_recording()`.
- `R/launch_sql.R` — same.
- `R/rebind.R` — `mr_hash()` errors for Shape B names; `mr_run/mr_variant/mr_as_of` gain Shape B routing.
- `R/launch.R` — `_mr_runs.outputs` entry shape for append_table.
- `NAMESPACE` — regenerated by `document()` (adds `prune_runs`, optional `grab(run=)`).
- `NEWS.md` — breaking-semantic-change note.
- `man/` — regenerated.

---

## Test helper convention

**Plan defect correction (found during Task 1):** The plan's test snippets use `.mr_with_temp_db({ <body> })` — that helper does not exist in this project. The real helper is `new_test_db()` in `tests/testthat/helper-testdb.R`, which is called as a single statement at the top of the `test_that()` body (it uses `withr::defer_parent` for cleanup; there is no block to wrap). Convert every occurrence:

```r
# What the plan shows:
test_that("...", {
  .mr_with_temp_db({
    <body>
  })
})

# What you write:
test_that("...", {
  new_test_db()
  <body>
})
```

Task 1's committed `tests/testthat/test-append-schema.R` is the reference pattern. Apply the same substitution silently in every subsequent task — do not flag it as a concern.

## Pre-flight

- [ ] **Step 0.1: Confirm baseline is clean**

Run:
```bash
cd /workspace/r-packages/modelrunnR && Rscript -e 'devtools::test()'
```
Expected: all tests pass (no pre-existing red).

- [ ] **Step 0.2: Confirm `R CMD check` baseline**

Run:
```bash
Rscript -e 'devtools::check(quiet = TRUE)'
```
Expected: 0 errors / 0 warnings. Notes are fine.

---

### Task 1: `_mr_append_tables` schema migration

**Files:**
- Modify: `R/schema.R` (add `.mr_migrate_append_tables`, call from `.mr_migrate`)
- Test: `tests/testthat/test-append-schema.R` (new)

- [ ] **Step 1.1: Write the failing test**

Create `tests/testthat/test-append-schema.R`:
```r
test_that("_mr_append_tables is created on connect with the expected columns", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    expect_true(DBI::dbExistsTable(con, "_mr_append_tables"))
    info <- DBI::dbGetQuery(con, "PRAGMA table_info(_mr_append_tables)")
    expect_setequal(
      info$name,
      c("logical_name", "physical_name", "schema_json",
        "first_seen", "last_seen", "row_count", "size_bytes")
    )
    pk <- info[info$pk == 1L, "name"]
    expect_identical(pk, "logical_name")
  })
})

test_that("migration is idempotent", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    # Force-run migrations a second time; must not error.
    expect_silent(.mr_migrate(con))
    expect_silent(.mr_migrate(con))
  })
})
```

Note: `.mr_with_temp_db()` already exists in `tests/testthat/helper-testdb.R`. Use it.

- [ ] **Step 1.2: Run the test and confirm it fails**

Run:
```bash
Rscript -e 'devtools::test(filter = "append-schema")'
```
Expected: FAIL with "`_mr_append_tables` does not exist".

- [ ] **Step 1.3: Implement the migration**

In `R/schema.R`, inside `.mr_migrate()`:
```r
.mr_migrate <- function(con) {
  .mr_migrate_runs(con)
  .mr_migrate_versions(con)
  .mr_migrate_artifacts(con)
  .mr_migrate_append_tables(con)
  invisible(NULL)
}
```

Add this function (place it under `.mr_migrate_artifacts`):
```r
.mr_migrate_append_tables <- function(con) {
  sql <- "
    CREATE TABLE IF NOT EXISTS _mr_append_tables (
      logical_name   TEXT PRIMARY KEY,
      physical_name  TEXT NOT NULL,
      schema_json    TEXT,
      first_seen     TIMESTAMP,
      last_seen      TIMESTAMP,
      row_count      BIGINT,
      size_bytes     BIGINT
    )
  "
  .mr_execute(con, sql)
  invisible(NULL)
}
```

- [ ] **Step 1.4: Run the test and confirm it passes**

Run:
```bash
Rscript -e 'devtools::test(filter = "append-schema")'
```
Expected: PASS.

- [ ] **Step 1.5: Commit**

```bash
git add R/schema.R tests/testthat/test-append-schema.R
git commit -m "$(cat <<'EOF'
feat(schema): add _mr_append_tables bookkeeping table

Shape B (run-indexed append log) registry. Per spec §1: one logical
name per row, physical_name + schema_json + row/byte counters. Created
idempotently on connect alongside existing migrations. Empty and unused
until stow() / grab() wiring lands in subsequent commits.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Namespace module — Shape lookup + guard

**Files:**
- Create: `R/namespace.R`
- Modify: `R/stow.R` (remove `.mr_guard_namespace` — moving to `namespace.R`)
- Test: `tests/testthat/test-append-schema.R` (add cases)

- [ ] **Step 2.1: Write the failing test**

Append to `tests/testthat/test-append-schema.R`:
```r
test_that(".mr_lookup_shape returns NULL for unknown name, 'A' for versioned, 'B' for append", {
  .mr_with_temp_db({
    expect_null(.mr_lookup_shape("nothing_here"))

    con <- .mr_get_connection()
    # Seed Shape A directly.
    DBI::dbExecute(con,
      "INSERT INTO _mr_versions (logical_name, content_hash, physical_name, kind,
                                 first_seen, last_seen, size_bytes)
       VALUES ('versioned_thing', 'h', 'versioned_thing__h', 'table',
               CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, 0)")
    expect_identical(.mr_lookup_shape("versioned_thing"), "A")

    # Seed Shape B directly.
    DBI::dbExecute(con,
      "INSERT INTO _mr_append_tables (logical_name, physical_name, schema_json,
                                       first_seen, last_seen, row_count, size_bytes)
       VALUES ('append_thing', 'append_thing__append', '{}',
               CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, 0, 0)")
    expect_identical(.mr_lookup_shape("append_thing"), "B")
  })
})

test_that(".mr_guard_namespace errors on cross-shape name collision", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    DBI::dbExecute(con,
      "INSERT INTO _mr_append_tables (logical_name, physical_name, schema_json,
                                       first_seen, last_seen, row_count, size_bytes)
       VALUES ('shared', 'shared__append', '{}',
               CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, 0, 0)")
    # Attempting to register Shape A under the same name must error.
    expect_error(
      .mr_guard_namespace("shared", shape = "A", new_kind = "artifact"),
      "already exists as an append"
    )
  })
})
```

- [ ] **Step 2.2: Run the test and confirm it fails**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-schema")'
```
Expected: FAIL — `.mr_lookup_shape` not found.

- [ ] **Step 2.3: Implement namespace module**

Create `R/namespace.R`:
```r
## Namespace: one logical name -> one storage shape.
##
## Shape A = content-addressed (backed by _mr_versions).
## Shape B = run-indexed append log (backed by _mr_append_tables).

.mr_lookup_shape <- function(name) {
  con <- .mr_get_connection()
  hit_a <- DBI::dbGetQuery(
    con,
    "SELECT 1 FROM _mr_versions WHERE logical_name = ? LIMIT 1",
    params = list(name)
  )
  if (nrow(hit_a) > 0L) return("A")
  hit_b <- DBI::dbGetQuery(
    con,
    "SELECT 1 FROM _mr_append_tables WHERE logical_name = ? LIMIT 1",
    params = list(name)
  )
  if (nrow(hit_b) > 0L) return("B")
  NULL
}

# Unified namespace guard. `shape` is the incoming shape ("A" or "B").
# `new_kind` is the within-shape kind (artifact/table/view for Shape A;
# ignored for Shape B which has only one kind).
.mr_guard_namespace <- function(name, shape = c("A", "B"),
                                new_kind = NULL, context = "stow") {
  shape <- match.arg(shape)
  existing <- .mr_lookup_shape(name)
  if (is.null(existing)) return(invisible(NULL))

  if (!identical(existing, shape)) {
    stop(sprintf(
      "%s(): '%s' already exists as %s; refusing to register it as %s.",
      context, name,
      if (existing == "A") "a versioned value" else "an append table",
      if (shape == "A") "a versioned value" else "an append table"
    ), call. = FALSE)
  }

  # Within Shape A, kinds (table/view/artifact) must also match.
  if (shape == "A" && !is.null(new_kind)) {
    con <- .mr_get_connection()
    existing_kinds <- DBI::dbGetQuery(
      con,
      "SELECT DISTINCT kind FROM _mr_versions WHERE logical_name = ?",
      params = list(name)
    )$kind
    if (length(existing_kinds) > 0L && !all(existing_kinds == new_kind)) {
      stop(sprintf(
        "%s(): '%s' already exists as a %s; refusing to register it as a %s. Use a different name or prune the existing versions first.",
        context, name, existing_kinds[1], new_kind
      ), call. = FALSE)
    }
  }
  invisible(NULL)
}
```

Remove the old `.mr_guard_namespace` from `R/stow.R` (lines 272–286). Update existing callers to the new signature:

In `R/stow.R`:
```r
# Before:
.mr_guard_namespace(name, "table")
# After:
.mr_guard_namespace(name, shape = "A", new_kind = "table")
```
(Same for `"artifact"`.)

In `R/rebind.R` (lines 92, 96):
```r
.mr_guard_namespace(name, shape = "A", new_kind = "table")
.mr_guard_namespace(name, shape = "A", new_kind = "artifact")
```

Search for other callers:
```bash
grep -rn ".mr_guard_namespace" R/
```
Update them all to the new signature.

- [ ] **Step 2.4: Run tests and confirm**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test()'
```
Expected: all pre-existing tests still pass + new namespace tests pass.

- [ ] **Step 2.5: Commit**

```bash
git add R/namespace.R R/stow.R R/rebind.R R/ingest.R R/mr_sql.R tests/testthat/test-append-schema.R
git commit -m "$(cat <<'EOF'
feat(namespace): shape-aware namespace guard + lookup

Adds .mr_lookup_shape() and unifies .mr_guard_namespace() across both
storage shapes. Cross-shape name collisions now error with a clear
message. Existing callers updated to the shape= / new_kind= signature.
No behavior change for existing Shape A paths.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Recording state carries `run_id` + `variant_label`

**Files:**
- Modify: `R/recording.R`
- Modify: `R/launch_one.R`, `R/launch_sql.R` (pass the new args)
- Test: `tests/testthat/test-append-stow.R` (new file — seed it here)

- [ ] **Step 3.1: Write the failing test**

Create `tests/testthat/test-append-stow.R`:
```r
test_that(".mr_recording_run_id returns NULL outside launch, run_id inside", {
  .mr_with_temp_db({
    expect_null(.mr_recording_run_id())
    expect_true(is.na(.mr_recording_variant_label()) ||
                is.null(.mr_recording_variant_label()))

    .mr_start_recording(run_id = "run_fake_123", variant_label = "lm")
    expect_identical(.mr_recording_run_id(), "run_fake_123")
    expect_identical(.mr_recording_variant_label(), "lm")
    .mr_stop_recording()

    expect_null(.mr_recording_run_id())
  })
})
```

- [ ] **Step 3.2: Run the test and confirm it fails**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-stow")'
```
Expected: FAIL — `.mr_recording_run_id` not found.

- [ ] **Step 3.3: Extend recording state**

Replace `R/recording.R` contents:
```r
## Per-launch recording context.
##
## While a recording context is active, every resolved read and write
## is captured as a `list(name, hash)` pair. The run row later
## serializes these lists to JSON in `_mr_runs.inputs` and
## `_mr_runs.outputs`.
##
## Also carries the current run_id and variant_label so Shape B stow
## can stamp `_mr_run_id` / `_mr_variant_label` system columns on the
## rows it appends.

.mr_start_recording <- function(run_id = NA_character_,
                                variant_label = NA_character_) {
  .mr_state$recording <- list(
    inputs        = list(),
    outputs       = list(),
    n_grabs       = 0L,
    n_stows       = 0L,
    run_id        = run_id,
    variant_label = variant_label
  )
  invisible(NULL)
}

.mr_stop_recording <- function() {
  rec <- .mr_state$recording
  .mr_state$recording <- NULL
  rec
}

.mr_is_recording <- function() {
  !is.null(.mr_state$recording)
}

# Read-only accessors for the current run_id / variant_label. Return
# NULL when there is no active recording context; otherwise the stored
# value (which may itself be NA when the caller didn't supply one).
.mr_recording_run_id <- function() {
  rec <- .mr_state$recording
  if (is.null(rec)) NULL else rec$run_id
}

.mr_recording_variant_label <- function() {
  rec <- .mr_state$recording
  if (is.null(rec)) NULL else rec$variant_label
}

.mr_pair <- function(name, hash) {
  list(name = name, hash = hash %||% NA_character_)
}

.mr_record_read <- function(name, hash = NA_character_) {
  if (!.mr_is_recording()) return(invisible(NULL))
  rec <- .mr_state$recording
  pair <- .mr_pair(name, hash)
  rec$n_grabs <- rec$n_grabs + 1L
  if (!.mr_pair_in(pair, rec$inputs)) {
    rec$inputs <- c(rec$inputs, list(pair))
  }
  .mr_state$recording <- rec
  invisible(NULL)
}

.mr_record_write <- function(name, hash = NA_character_) {
  if (!.mr_is_recording()) return(invisible(NULL))
  rec <- .mr_state$recording
  pair <- .mr_pair(name, hash)
  rec$n_stows <- rec$n_stows + 1L
  if (!.mr_pair_in(pair, rec$outputs)) {
    rec$outputs <- c(rec$outputs, list(pair))
  }
  .mr_state$recording <- rec
  invisible(NULL)
}

.mr_pair_in <- function(pair, pairs) {
  for (p in pairs) {
    if (identical(p$name, pair$name) && identical(p$hash, pair$hash)) return(TRUE)
  }
  FALSE
}

`%||%` <- function(x, y) if (is.null(x)) y else x
```

In `R/launch_one.R`, change line 61 from:
```r
.mr_start_recording()
```
to:
```r
.mr_start_recording(run_id = run_id, variant_label = label)
```

In `R/launch_sql.R`, find the corresponding `.mr_start_recording()` call and add the same args. Also grep for any other calls:
```bash
grep -rn "\.mr_start_recording(" R/
```
Update each.

- [ ] **Step 3.4: Run tests**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test()'
```
Expected: full suite still green + new recording test passes.

- [ ] **Step 3.5: Commit**

```bash
git add R/recording.R R/launch_one.R R/launch_sql.R tests/testthat/test-append-stow.R
git commit -m "$(cat <<'EOF'
feat(recording): carry run_id + variant_label through recording state

Extends the per-launch recording context with the active run_id and
variant_label so downstream writers (Shape B stow in subsequent
commits) can stamp system columns on inserted rows without reaching
back into the launch machinery. Adds read-only accessors; no change to
read/write recording semantics.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Shape B — ensure_table + first-write append

**Files:**
- Create: `R/shape_append.R`
- Test: `tests/testthat/test-append-stow.R` (extend)

- [ ] **Step 4.1: Write the failing test**

Add to `tests/testthat/test-append-stow.R`:
```r
test_that(".mr_append_write_frame creates the physical table on first write", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    .mr_start_recording(run_id = "run_1", variant_label = "lm")
    on.exit(.mr_stop_recording(), add = TRUE)

    df <- data.frame(model = "lm", rmse = 0.5, r2 = 0.9, stringsAsFactors = FALSE)
    .mr_append_write_frame("metrics", df)

    expect_true(DBI::dbExistsTable(con, "metrics__append"))
    reg <- DBI::dbGetQuery(con, "SELECT * FROM _mr_append_tables WHERE logical_name = 'metrics'")
    expect_identical(nrow(reg), 1L)
    expect_identical(reg$physical_name, "metrics__append")
    expect_identical(reg$row_count, bit64::as.integer64(1L))

    rows <- DBI::dbGetQuery(con, "SELECT * FROM metrics__append")
    expect_identical(nrow(rows), 1L)
    expect_setequal(
      colnames(rows),
      c("model", "rmse", "r2", "_mr_run_id", "_mr_variant_label")
    )
    expect_identical(rows[["_mr_run_id"]], "run_1")
    expect_identical(rows[["_mr_variant_label"]], "lm")
  })
})

test_that("second write appends under existing physical table", {
  .mr_with_temp_db({
    con <- .mr_get_connection()

    .mr_start_recording(run_id = "run_1", variant_label = "lm")
    .mr_append_write_frame("metrics",
      data.frame(model = "lm", rmse = 0.5, stringsAsFactors = FALSE))
    .mr_stop_recording()

    .mr_start_recording(run_id = "run_2", variant_label = "rf")
    .mr_append_write_frame("metrics",
      data.frame(model = "rf", rmse = 0.4, stringsAsFactors = FALSE))
    .mr_stop_recording()

    rows <- DBI::dbGetQuery(con,
      "SELECT * FROM metrics__append ORDER BY _mr_run_id")
    expect_identical(nrow(rows), 2L)
    expect_identical(rows[["_mr_run_id"]], c("run_1", "run_2"))
    expect_identical(rows[["_mr_variant_label"]], c("lm", "rf"))
  })
})
```

- [ ] **Step 4.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-stow")'
```
Expected: FAIL — `.mr_append_write_frame` not found.

- [ ] **Step 4.3: Implement**

Create `R/shape_append.R`:
```r
## Shape B — run-indexed append log.
##
## Per spec §1 / §3:
##   - One physical table per logical name: <logical>__append.
##   - Two system columns appended to user columns: _mr_run_id,
##     _mr_variant_label.
##   - Registered in _mr_append_tables; the schema_json field records
##     the column -> type map of the CURRENT user-facing schema (system
##     columns excluded).

.mr_append_physical_name <- function(name) {
  paste0(name, "__append")
}

.mr_append_reserved_cols <- c("_mr_run_id", "_mr_variant_label")

# Convert a data frame's column types to DuckDB types, returning a
# list(col = type, ...). Used to build the CREATE TABLE and to populate
# schema_json.
.mr_append_frame_types <- function(df) {
  setNames(
    lapply(df, .mr_append_r_to_duckdb_type),
    names(df)
  )
}

.mr_append_r_to_duckdb_type <- function(col) {
  if (is.integer(col))   return("INTEGER")
  if (is.numeric(col))   return("DOUBLE")
  if (is.logical(col))   return("BOOLEAN")
  if (inherits(col, "POSIXct")) return("TIMESTAMP")
  if (inherits(col, "Date"))    return("DATE")
  if (is.character(col)) return("TEXT")
  if (is.factor(col))    return("TEXT")
  "TEXT"
}

# First-write path: create the physical table with user columns +
# system columns, insert the row, register in _mr_append_tables.
.mr_append_ensure_table <- function(con, name, frame_types) {
  physical <- .mr_append_physical_name(name)
  cols <- c(
    vapply(names(frame_types),
           function(c) sprintf("%s %s", .mr_quote_ident(c), frame_types[[c]]),
           character(1)),
    "_mr_run_id TEXT",
    "_mr_variant_label TEXT"
  )
  .mr_execute(
    con,
    sprintf("CREATE TABLE IF NOT EXISTS %s (%s)",
            .mr_quote_ident(physical),
            paste(cols, collapse = ", "))
  )
  schema_json <- jsonlite::toJSON(frame_types, auto_unbox = TRUE)
  now <- Sys.time()
  DBI::dbExecute(
    con,
    "INSERT INTO _mr_append_tables
       (logical_name, physical_name, schema_json,
        first_seen, last_seen, row_count, size_bytes)
     VALUES (?, ?, ?, ?, ?, 0, 0)",
    params = list(name, physical, as.character(schema_json), now, now)
  )
  physical
}

# Top-level append for materialized frames. Caller MUST be inside an
# active recording context (.mr_start_recording) so run_id / variant_label
# are available.
.mr_append_write_frame <- function(name, value) {
  run_id <- .mr_recording_run_id()
  if (is.null(run_id) || is.na(run_id)) {
    stop(
      "stow(): append writes require an active launch() context; ",
      "stow() outside launch() is not supported for data frames in v0.1.",
      call. = FALSE
    )
  }
  label <- .mr_recording_variant_label()
  if (is.null(label)) label <- NA_character_

  .mr_append_guard_reserved_cols(name, value)

  con <- .mr_get_connection()
  physical <- .mr_append_physical_name(name)
  frame_types <- .mr_append_frame_types(value)

  now <- Sys.time()
  DBI::dbBegin(con)
  tryCatch({
    registered <- DBI::dbGetQuery(
      con,
      "SELECT * FROM _mr_append_tables WHERE logical_name = ?",
      params = list(name)
    )
    if (nrow(registered) == 0L) {
      .mr_append_ensure_table(con, name, frame_types)
    } else {
      # Reconciliation (columns added / dropped / type conflict) lands
      # in a subsequent task. First-cut behavior: require exact schema
      # match. Reconciliation will relax this.
      .mr_append_require_schema_match(registered, value)
    }

    # Stamp system columns and insert. `dbAppendTable` matches by name
    # so column order in `value` is irrelevant.
    to_insert <- value
    to_insert[["_mr_run_id"]]         <- run_id
    to_insert[["_mr_variant_label"]]  <- label
    DBI::dbAppendTable(con, physical, to_insert)

    size_bytes <- as.numeric(object.size(value))
    DBI::dbExecute(
      con,
      "UPDATE _mr_append_tables
          SET row_count  = row_count + ?,
              size_bytes = size_bytes + ?,
              last_seen  = ?
        WHERE logical_name = ?",
      params = list(nrow(value), size_bytes, now, name)
    )
    DBI::dbCommit(con)
  }, error = function(e) {
    DBI::dbRollback(con)
    stop(e)
  })

  chunk_hash <- .mr_append_row_hash(value)
  .mr_record_write(name, chunk_hash)
  invisible(chunk_hash)
}

.mr_append_guard_reserved_cols <- function(name, value) {
  conflict <- intersect(colnames(value), .mr_append_reserved_cols)
  if (length(conflict) > 0L) {
    stop(sprintf(
      "stow('%s'): column%s %s %s reserved; rename before stowing.",
      name,
      if (length(conflict) == 1L) "" else "s",
      paste(sprintf("'%s'", conflict), collapse = ", "),
      if (length(conflict) == 1L) "is" else "are"
    ), call. = FALSE)
  }
  invisible(NULL)
}

# Placeholder — tightened in the schema-drift task.
.mr_append_require_schema_match <- function(registered_row, value) {
  schema <- jsonlite::fromJSON(registered_row$schema_json[1], simplifyVector = FALSE)
  incoming_cols <- names(value)
  known_cols    <- names(schema)
  if (!setequal(incoming_cols, known_cols)) {
    stop(sprintf(
      "stow(): incoming columns {%s} do not match stored schema {%s}. Schema reconciliation not yet implemented.",
      paste(incoming_cols, collapse = ", "),
      paste(known_cols,    collapse = ", ")
    ), call. = FALSE)
  }
  invisible(NULL)
}

# Stable, row-order-independent hash of the rows this call contributed.
# Not user-facing; used in _mr_runs.outputs provenance (spec §8).
.mr_append_row_hash <- function(value) {
  .mr_hash_bytes(serialize(value[do.call(order, value), , drop = FALSE], NULL))
}
```

- [ ] **Step 4.4: Run tests**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-stow")'
```
Expected: PASS.

- [ ] **Step 4.5: Commit**

```bash
git add R/shape_append.R tests/testthat/test-append-stow.R
git commit -m "$(cat <<'EOF'
feat(append): write frames into run-indexed append log (Shape B)

Implements .mr_append_write_frame() — creates <logical>__append on
first write, appends on subsequent writes, stamps _mr_run_id and
_mr_variant_label from the active recording context. Schema drift
reconciliation comes in the next commit; this first cut requires
exact schema match on the second write onward. stow() dispatch still
routes data frames to Shape A — that wiring lands after reconciliation.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Schema reconciliation — columns added

**Files:**
- Modify: `R/shape_append.R` (replace `.mr_append_require_schema_match` with `.mr_append_reconcile_schema`)
- Test: `tests/testthat/test-append-schema-drift.R` (new)

- [ ] **Step 5.1: Write the failing test**

Create `tests/testthat/test-append-schema-drift.R`:
```r
test_that("incoming frame with extra columns triggers ALTER TABLE ADD + NULL backfill", {
  .mr_with_temp_db({
    con <- .mr_get_connection()

    .mr_start_recording(run_id = "run_1", variant_label = "lm")
    .mr_append_write_frame("metrics",
      data.frame(model = "lm", rmse = 0.5, stringsAsFactors = FALSE))
    .mr_stop_recording()

    .mr_start_recording(run_id = "run_2", variant_label = "rf")
    expect_warning(
      .mr_append_write_frame("metrics",
        data.frame(model = "rf", rmse = 0.4, mae = 0.3, r2 = 0.85,
                   stringsAsFactors = FALSE)),
      "extending schema"
    )
    .mr_stop_recording()

    rows <- DBI::dbGetQuery(con,
      "SELECT * FROM metrics__append ORDER BY _mr_run_id")
    expect_identical(nrow(rows), 2L)
    expect_true(all(c("model", "rmse", "mae", "r2") %in% colnames(rows)))
    # The prior run's row has NULL for the two new columns.
    expect_true(is.na(rows$mae[1]))
    expect_true(is.na(rows$r2[1]))
    expect_identical(rows$mae[2], 0.3)
    expect_identical(rows$r2[2],  0.85)

    # schema_json is updated to include mae + r2.
    reg <- DBI::dbGetQuery(con,
      "SELECT schema_json FROM _mr_append_tables WHERE logical_name = 'metrics'")
    schema <- jsonlite::fromJSON(reg$schema_json[1], simplifyVector = FALSE)
    expect_setequal(names(schema), c("model", "rmse", "mae", "r2"))
  })
})
```

- [ ] **Step 5.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-schema-drift")'
```
Expected: FAIL — current placeholder errors on mismatched columns.

- [ ] **Step 5.3: Implement reconciliation (add-columns branch)**

In `R/shape_append.R`, replace `.mr_append_require_schema_match` with:
```r
# Reconcile the incoming frame's columns against the stored schema.
# Returns the (possibly extended) stored schema list. Performs any
# ALTER TABLE ADD COLUMN operations and updates _mr_append_tables's
# schema_json inside the caller's transaction.
.mr_append_reconcile_schema <- function(con, name, registered_row, value) {
  physical <- registered_row$physical_name[1]
  schema   <- jsonlite::fromJSON(registered_row$schema_json[1], simplifyVector = FALSE)
  incoming_types <- .mr_append_frame_types(value)

  added   <- setdiff(names(incoming_types), names(schema))
  missing <- setdiff(names(schema), names(incoming_types))

  if (length(added) > 0L) {
    warning(sprintf(
      "stow('%s'): extending schema with new column%s: %s",
      name,
      if (length(added) == 1L) "" else "s",
      paste(added, collapse = ", ")
    ), call. = FALSE)
    for (col in added) {
      type <- incoming_types[[col]]
      .mr_execute(
        con,
        sprintf("ALTER TABLE %s ADD COLUMN %s %s",
                .mr_quote_ident(physical),
                .mr_quote_ident(col),
                type)
      )
      schema[[col]] <- type
    }
  }

  if (length(missing) > 0L) {
    message(sprintf(
      "stow('%s'): incoming data missing column%s %s; inserted as NULL",
      name,
      if (length(missing) == 1L) "" else "s",
      paste(sprintf("'%s'", missing), collapse = ", ")
    ))
  }

  if (length(added) > 0L) {
    schema_json <- jsonlite::toJSON(schema, auto_unbox = TRUE)
    DBI::dbExecute(
      con,
      "UPDATE _mr_append_tables SET schema_json = ? WHERE logical_name = ?",
      params = list(as.character(schema_json), name)
    )
  }

  schema
}
```

Update `.mr_append_write_frame` to call it (replace the `.mr_append_require_schema_match(...)` line):
```r
    if (nrow(registered) == 0L) {
      .mr_append_ensure_table(con, name, frame_types)
    } else {
      .mr_append_reconcile_schema(con, name, registered, value)
    }
```

`dbAppendTable` matches by column name and fills missing columns with NULL, so the missing-columns path needs no extra code (just the `message()` in the reconciler).

- [ ] **Step 5.4: Run tests**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-(stow|schema-drift)")'
```
Expected: PASS.

- [ ] **Step 5.5: Commit**

```bash
git add R/shape_append.R tests/testthat/test-append-schema-drift.R
git commit -m "$(cat <<'EOF'
feat(append): extend schema on incoming new columns

Incoming columns absent from the stored schema trigger ALTER TABLE ADD
COLUMN, backfill prior rows as NULL, update schema_json, and emit a
warning. Per spec §4: never lose a run's work to a schema mismatch.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Schema reconciliation — missing columns

**Files:**
- Test: `tests/testthat/test-append-schema-drift.R` (add case)

- [ ] **Step 6.1: Write the failing test**

Append to `tests/testthat/test-append-schema-drift.R`:
```r
test_that("incoming frame dropping columns inserts NULLs and emits a message", {
  .mr_with_temp_db({
    con <- .mr_get_connection()

    .mr_start_recording(run_id = "run_1", variant_label = "lm")
    .mr_append_write_frame("metrics",
      data.frame(model = "lm", rmse = 0.5, mae = 0.3, stringsAsFactors = FALSE))
    .mr_stop_recording()

    .mr_start_recording(run_id = "run_2", variant_label = "rf")
    expect_message(
      .mr_append_write_frame("metrics",
        data.frame(model = "rf", rmse = 0.4, stringsAsFactors = FALSE)),
      "missing column 'mae'"
    )
    .mr_stop_recording()

    rows <- DBI::dbGetQuery(con,
      "SELECT * FROM metrics__append ORDER BY _mr_run_id")
    expect_identical(nrow(rows), 2L)
    expect_identical(rows$mae, c(0.3, NA_real_))
  })
})
```

- [ ] **Step 6.2: Run and confirm it already passes**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-schema-drift")'
```
Expected: PASS — the reconciler already messages; `dbAppendTable` fills missing columns.

If it fails, inspect — `dbAppendTable` may require the column to be present as NA. Fix by setting the missing columns to NA on the `to_insert` frame before insertion:
```r
# In .mr_append_write_frame, after reconcile:
schema <- .mr_append_reconcile_schema(con, name, registered, value)
for (col in setdiff(names(schema), names(value))) {
  to_insert[[col]] <- NA
}
```
(Hoist the `schema <- ...` assignment up from the reconciler's return value.)

- [ ] **Step 6.3: Commit**

```bash
git add tests/testthat/test-append-schema-drift.R R/shape_append.R
git commit -m "$(cat <<'EOF'
test(append): incoming frames missing columns insert NULL

Adds the missing-columns case to schema-drift tests. If dbAppendTable
requires columns to be present, the to_insert frame now fills them
with NA before insert. Either way, per spec §4: never lose a run.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Schema reconciliation — type conflict (coerce-to-TEXT)

Spec §4.1 picks option (a): on type mismatch, coerce the column to TEXT (both the existing stored column and the incoming data), updating `schema_json`.

**Files:**
- Modify: `R/shape_append.R`
- Test: `tests/testthat/test-append-schema-drift.R`

- [ ] **Step 7.1: Write the failing test**

Append to `tests/testthat/test-append-schema-drift.R`:
```r
test_that("type conflict on a column coerces that column to TEXT (both existing and incoming)", {
  .mr_with_temp_db({
    con <- .mr_get_connection()

    .mr_start_recording(run_id = "run_1", variant_label = "lm")
    .mr_append_write_frame("metrics",
      data.frame(model = "lm", score = 0.5, stringsAsFactors = FALSE))
    .mr_stop_recording()

    .mr_start_recording(run_id = "run_2", variant_label = "rf")
    expect_warning(
      .mr_append_write_frame("metrics",
        data.frame(model = "rf", score = "N/A", stringsAsFactors = FALSE)),
      "type conflict"
    )
    .mr_stop_recording()

    rows <- DBI::dbGetQuery(con,
      "SELECT model, score FROM metrics__append ORDER BY _mr_run_id")
    expect_type(rows$score, "character")
    expect_identical(rows$score, c("0.5", "N/A"))

    reg <- DBI::dbGetQuery(con,
      "SELECT schema_json FROM _mr_append_tables WHERE logical_name = 'metrics'")
    schema <- jsonlite::fromJSON(reg$schema_json[1], simplifyVector = FALSE)
    expect_identical(schema$score, "TEXT")
  })
})
```

- [ ] **Step 7.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-schema-drift")'
```
Expected: FAIL — current reconciler doesn't handle type conflicts.

- [ ] **Step 7.3: Implement type-conflict branch**

In `R/shape_append.R`, extend `.mr_append_reconcile_schema` (inside the common-columns loop, after handling added/missing):
```r
  # Type-conflict resolution — coerce offending column to TEXT
  # (spec §4.1 option a). Cast the stored column and coerce the
  # incoming column; update schema_json.
  common <- intersect(names(schema), names(incoming_types))
  conflicts <- common[vapply(common,
    function(c) !identical(schema[[c]], incoming_types[[c]]),
    logical(1))]

  for (col in conflicts) {
    warning(sprintf(
      "stow('%s'): type conflict on column '%s' (stored %s, incoming %s); coercing column to TEXT.",
      name, col, schema[[col]], incoming_types[[col]]
    ), call. = FALSE)
    # DuckDB allows ALTER TABLE ... ALTER COLUMN ... SET DATA TYPE with a
    # USING expression to cast existing rows.
    .mr_execute(
      con,
      sprintf(
        "ALTER TABLE %s ALTER COLUMN %s SET DATA TYPE TEXT USING CAST(%s AS TEXT)",
        .mr_quote_ident(physical),
        .mr_quote_ident(col),
        .mr_quote_ident(col)
      )
    )
    schema[[col]] <- "TEXT"
  }

  # Incoming data for conflicted columns must be coerced so
  # dbAppendTable binds them as TEXT.
  if (length(conflicts) > 0L) {
    # Attach to registered_row's environment via attribute so caller
    # can pick it up cleanly.
    attr(schema, "coerce_to_text") <- conflicts
  }
```

And update `.mr_append_write_frame`'s flow to apply the coercion on `to_insert` before insert:
```r
    if (nrow(registered) == 0L) {
      .mr_append_ensure_table(con, name, frame_types)
      schema <- frame_types
    } else {
      schema <- .mr_append_reconcile_schema(con, name, registered, value)
    }

    to_insert <- value
    coerce <- attr(schema, "coerce_to_text")
    if (!is.null(coerce)) {
      for (col in coerce) {
        to_insert[[col]] <- as.character(to_insert[[col]])
      }
    }
    for (col in setdiff(names(schema), names(to_insert))) {
      to_insert[[col]] <- NA
    }
    to_insert[["_mr_run_id"]]         <- run_id
    to_insert[["_mr_variant_label"]]  <- label
    DBI::dbAppendTable(con, physical, to_insert)
```

Also update the schema_json persistence to happen whenever ANY change occurred (added OR conflicts), not just `length(added) > 0L`:
```r
  if (length(added) > 0L || length(conflicts) > 0L) {
    schema_json <- jsonlite::toJSON(
      setNames(lapply(names(schema), function(n) schema[[n]]), names(schema)),
      auto_unbox = TRUE
    )
    DBI::dbExecute(
      con,
      "UPDATE _mr_append_tables SET schema_json = ? WHERE logical_name = ?",
      params = list(as.character(schema_json), name)
    )
  }
```

- [ ] **Step 7.4: Run tests**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-schema-drift")'
```
Expected: PASS. If `ALTER TABLE ... ALTER COLUMN ... SET DATA TYPE` syntax is rejected by the installed DuckDB build, fall back to the portable pattern: add a new `<col>__text` column, UPDATE-cast existing rows into it, drop the old column, rename the new column. Test against your DuckDB version first and adjust inline.

- [ ] **Step 7.5: Commit**

```bash
git add R/shape_append.R tests/testthat/test-append-schema-drift.R
git commit -m "$(cat <<'EOF'
feat(append): coerce column to TEXT on type conflict

Per spec §4.1 option (a): when an incoming frame's column has a
different DuckDB type than the stored column, cast the stored
column to TEXT, coerce the incoming column to character, warn.
Lossless — values survive as strings. Users who want to restart
the table can drop it (helper deferred per spec §9).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Reserved-column guard (system columns in user frame)

**Files:**
- Test: `tests/testthat/test-append-stow.R`

- [ ] **Step 8.1: Write the failing test**

Append to `tests/testthat/test-append-stow.R`:
```r
test_that("stowing a frame with reserved system columns errors pre-insert", {
  .mr_with_temp_db({
    con <- .mr_get_connection()

    .mr_start_recording(run_id = "run_1", variant_label = "lm")
    on.exit(.mr_stop_recording(), add = TRUE)

    bad <- data.frame(
      model      = "lm",
      `_mr_run_id` = "fake",
      check.names = FALSE,
      stringsAsFactors = FALSE
    )
    expect_error(
      .mr_append_write_frame("metrics", bad),
      "'_mr_run_id' (is|are) reserved"
    )

    # No physical table created, no registry row written.
    expect_false(DBI::dbExistsTable(con, "metrics__append"))
    reg <- DBI::dbGetQuery(con,
      "SELECT * FROM _mr_append_tables WHERE logical_name = 'metrics'")
    expect_identical(nrow(reg), 0L)
  })
})
```

- [ ] **Step 8.2: Run — expect PASS already**

The guard already runs at the top of `.mr_append_write_frame`.

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-stow")'
```
Expected: PASS (guard fires before any DB work).

- [ ] **Step 8.3: Commit**

```bash
git add tests/testthat/test-append-stow.R
git commit -m "$(cat <<'EOF'
test(append): verify reserved-column guard fires pre-insert

No physical table and no registry row are written when the incoming
frame already has _mr_run_id / _mr_variant_label — invariant: a run
that reaches stow() can always save its work.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Wire `stow()` data.frame branch to Shape B

**Files:**
- Modify: `R/stow.R`
- Test: `tests/testthat/test-append-stow.R`

- [ ] **Step 9.1: Write the failing test**

Append to `tests/testthat/test-append-stow.R`:
```r
test_that("stow(df, name) inside launch() routes to Shape B", {
  .mr_with_temp_db({
    con <- .mr_get_connection()

    launch({
      stow(data.frame(model = "lm", rmse = 0.5, stringsAsFactors = FALSE),
           "metrics")
    }, label = "lm")

    launch({
      stow(data.frame(model = "rf", rmse = 0.4, stringsAsFactors = FALSE),
           "metrics")
    }, label = "rf")

    expect_true(DBI::dbExistsTable(con, "metrics__append"))
    rows <- DBI::dbGetQuery(con,
      "SELECT * FROM metrics__append ORDER BY _mr_variant_label")
    expect_identical(nrow(rows), 2L)
    expect_setequal(rows[["_mr_variant_label"]], c("lm", "rf"))

    # _mr_versions must NOT have metrics (Shape A) rows.
    versions <- DBI::dbGetQuery(con,
      "SELECT * FROM _mr_versions WHERE logical_name = 'metrics'")
    expect_identical(nrow(versions), 0L)
  })
})

test_that("stow(obj, name) for non-tabular values still goes to Shape A", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    launch({
      stow(list(a = 1, b = 2), "my_model")
    }, label = "x")

    versions <- DBI::dbGetQuery(con,
      "SELECT * FROM _mr_versions WHERE logical_name = 'my_model'")
    expect_identical(nrow(versions), 1L)
    expect_identical(versions$kind, "artifact")

    # And _mr_append_tables has nothing.
    reg <- DBI::dbGetQuery(con,
      "SELECT * FROM _mr_append_tables WHERE logical_name = 'my_model'")
    expect_identical(nrow(reg), 0L)
  })
})
```

- [ ] **Step 9.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-stow")'
```
Expected: FAIL — `stow()` still routes data frames to Shape A (`_mr_stow_table`).

- [ ] **Step 9.3: Flip stow() dispatch**

In `R/stow.R`, modify the `stow()` body:
```r
stow <- function(value, name) {
  if (missing(name) && is.character(value) && length(value) == 1L) {
    stop(
      "stow() is value-first as of this version: stow(value, name). ",
      "Did you mean `stow(<value>, \"", value, "\")` ?",
      call. = FALSE
    )
  }
  .mr_validate_name(name, context = "stow")

  if (inherits(value, "tbl_lazy")) {
    .mr_guard_namespace(name, shape = "B")
    .mr_append_write_lazy(name, value)   # new in task 10
  } else if (is.data.frame(value)) {
    .mr_guard_namespace(name, shape = "B")
    .mr_append_write_frame(name, value)
  } else {
    .mr_guard_namespace(name, shape = "A", new_kind = "artifact")
    .mr_stow_artifact(name, value)
  }
  invisible(value)
}
```

Note: `.mr_append_write_lazy` doesn't exist yet (task 10). The test in this task only exercises the data.frame path, so failing import at load_all time isn't a risk as long as the lazy-tbl branch isn't hit. If load_all complains about the undefined symbol, stub it:
```r
.mr_append_write_lazy <- function(name, value) {
  stop("stow(tbl_lazy) Shape B wiring lands in a subsequent commit.",
       call. = FALSE)
}
```
(Place the stub at the bottom of `R/shape_append.R`; remove in task 10.)

`.mr_stow_table` and `.mr_stow_lazy` (Shape A tabular writers) are now unreachable from `stow()`. Do NOT delete them yet — `ingest()` still calls `.mr_stow_table` (see `R/ingest.R`), and `rebind.R` still calls `.mr_stow_table` / `.mr_stow_artifact` for bare-value rebinds. Leave them until the queued §12 reorg.

- [ ] **Step 9.4: Run full suite**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test()'
```
Expected: new Shape B tests pass. Pre-existing tests that specifically exercise `stow(df, ...)` outside `launch()` or assert on `_mr_versions` rows for data-frame stows WILL fail — this is the intended semantic break. Catalogue failing tests and update them to match the new contract in Task 10 (after lazy wiring) or defer to Task 14's cleanup.

To scope what breaks cleanly, skip the broken tests for now with:
```r
skip("append-mode stow: expected to rewrite for Shape B in task 14")
```
at the top of each affected test, and enumerate them in the commit message.

- [ ] **Step 9.5: Commit**

```bash
git add R/stow.R R/shape_append.R tests/testthat/test-append-stow.R tests/testthat/test-*.R
git commit -m "$(cat <<'EOF'
feat(stow): route data frames to Shape B append log

stow(df, name) inside launch() now appends to <name>__append with
run_id + variant_label stamping. Non-tabular stow() unchanged (Shape
A). stow(tbl_lazy) is stubbed until the next commit.

Pre-existing tests asserting _mr_versions rows for data-frame stows
are skipped and queued for rewrite in task 14. Shape A data-frame
writers (.mr_stow_table, .mr_stow_lazy) remain for ingest() and
rebind() callers.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: Shape B lazy-tbl path (server-side INSERT ... SELECT)

**Files:**
- Modify: `R/shape_append.R`
- Test: `tests/testthat/test-append-stow.R`

- [ ] **Step 10.1: Write the failing test**

Append to `tests/testthat/test-append-stow.R`:
```r
test_that("stow(tbl_lazy, name) materializes server-side into Shape B", {
  .mr_with_temp_db({
    con <- .mr_get_connection()

    # Seed an ingested source to have something to dbplyr over.
    src <- tempfile(fileext = ".csv")
    write.csv(data.frame(model = c("lm","rf"), rmse = c(0.5, 0.4)),
              src, row.names = FALSE)
    ingest("src", src)

    launch({
      t <- grab("src")
      t |>
        dplyr::filter(rmse < 0.5) |>
        stow("filtered_metrics")
    }, label = "lm")

    expect_true(DBI::dbExistsTable(con, "filtered_metrics__append"))
    rows <- DBI::dbGetQuery(con, "SELECT * FROM filtered_metrics__append")
    expect_identical(nrow(rows), 1L)
    expect_identical(rows$model, "rf")
    expect_false(is.na(rows[["_mr_run_id"]][1]))
    expect_identical(rows[["_mr_variant_label"]][1], "lm")
  })
})
```

- [ ] **Step 10.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-stow")'
```
Expected: FAIL (stub errors).

- [ ] **Step 10.3: Implement the lazy path**

In `R/shape_append.R`, replace the `.mr_append_write_lazy` stub:
```r
.mr_append_write_lazy <- function(name, value) {
  run_id <- .mr_recording_run_id()
  if (is.null(run_id) || is.na(run_id)) {
    stop(
      "stow(): append writes require an active launch() context; ",
      "stow() outside launch() is not supported for lazy tbls in v0.1.",
      call. = FALSE
    )
  }
  label <- .mr_recording_variant_label()
  if (is.null(label)) label <- NA_character_

  con <- .mr_get_connection()
  remote <- dbplyr::remote_con(value)
  if (!identical(remote, con)) {
    stop(
      "stow(): lazy tbl is bound to a different DBI connection; ",
      "call dplyr::collect() first and stow the materialized result.",
      call. = FALSE
    )
  }

  # Peek the lazy tbl's column types via a zero-row collect — lets us
  # reuse the reconciliation path without materializing the full result
  # twice.
  zero_head <- value |> head(0) |> dplyr::collect()
  .mr_append_guard_reserved_cols(name, zero_head)

  frame_types <- .mr_append_frame_types(zero_head)
  sql_body <- as.character(dbplyr::sql_render(value))
  physical <- .mr_append_physical_name(name)
  now <- Sys.time()

  DBI::dbBegin(con)
  tryCatch({
    registered <- DBI::dbGetQuery(
      con,
      "SELECT * FROM _mr_append_tables WHERE logical_name = ?",
      params = list(name)
    )
    if (nrow(registered) == 0L) {
      .mr_append_ensure_table(con, name, frame_types)
      schema <- frame_types
    } else {
      # Lazy path's reconciler parallels the frame path; the
      # "coerce to TEXT" attribute path isn't exercised yet because
      # the INSERT...SELECT can CAST in SQL. Errors on type mismatch
      # for v0.1 to keep the scope tight; queue as follow-up.
      schema <- .mr_append_reconcile_schema(con, name, registered, zero_head)
      coerce <- attr(schema, "coerce_to_text")
      if (!is.null(coerce)) {
        stop(sprintf(
          "stow(<lazy tbl>, '%s'): type conflict on column(s) %s; lazy-path coercion deferred to v0.2. Collect() and stow the materialized frame.",
          name, paste(coerce, collapse = ", ")
        ), call. = FALSE)
      }
    }

    # INSERT ... SELECT with system columns projected. Column order
    # mirrors the schema-json order + system columns to match the
    # physical table layout.
    user_cols <- names(schema)
    col_list <- c(
      vapply(user_cols, .mr_quote_ident, character(1)),
      ".mr_run_id", ".mr_variant_label"  # placeholders; rewritten below
    )
    # Build explicit column list for INSERT target:
    insert_cols <- paste(c(
      vapply(user_cols, .mr_quote_ident, character(1)),
      "\"_mr_run_id\"", "\"_mr_variant_label\""
    ), collapse = ", ")
    # Build the SELECT projection: only user cols come from the
    # sub-query, system cols are literals.
    select_user <- paste(
      vapply(user_cols, .mr_quote_ident, character(1)),
      collapse = ", "
    )

    sql <- sprintf(
      "INSERT INTO %s (%s) SELECT %s, %s, %s FROM (%s) AS _src",
      .mr_quote_ident(physical),
      insert_cols,
      select_user,
      DBI::dbQuoteLiteral(con, run_id),
      if (is.na(label)) "NULL" else DBI::dbQuoteLiteral(con, as.character(label)),
      sql_body
    )
    rows_inserted <- DBI::dbExecute(con, sql)

    DBI::dbExecute(
      con,
      "UPDATE _mr_append_tables
          SET row_count = row_count + ?, last_seen = ?
        WHERE logical_name = ?",
      params = list(rows_inserted, now, name)
    )
    DBI::dbCommit(con)
  }, error = function(e) {
    DBI::dbRollback(con)
    stop(e)
  })

  # Chunk hash for the lazy path: hash the SQL text since we don't
  # have the rows in R. This is weaker than the frame-path row hash
  # but matches the "provenance only" role per spec §8.
  chunk_hash <- .mr_hash_bytes(charToRaw(sql_body))
  .mr_record_write(name, chunk_hash)
  invisible(chunk_hash)
}
```

- [ ] **Step 10.4: Run tests**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-stow")'
```
Expected: PASS.

- [ ] **Step 10.5: Commit**

```bash
git add R/shape_append.R tests/testthat/test-append-stow.R
git commit -m "$(cat <<'EOF'
feat(stow): server-side Shape B append for lazy tbls

stow(tbl_lazy, name) materializes via INSERT INTO <physical>(...)
SELECT ... , <run_id>, <label> FROM (<rendered sql>). Reserved-column
guard and schema reconciliation reuse the frame-path helpers. Type
coercion in the lazy path is deferred to v0.2 (errors with a
collect-and-retry hint).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: `grab()` Shape B dispatch + launch-context default

**Files:**
- Modify: `R/grab.R`
- Modify: `R/shape_append.R` (add `.mr_append_read`)
- Test: `tests/testthat/test-append-grab.R` (new)

- [ ] **Step 11.1: Write the failing test**

Create `tests/testthat/test-append-grab.R`:
```r
test_that("grab(name) inside launch() returns current-run rows only", {
  .mr_with_temp_db({
    launch({ stow(data.frame(model = "lm", rmse = 0.5), "metrics") }, label = "lm")
    launch({ stow(data.frame(model = "rf", rmse = 0.4), "metrics") }, label = "rf")

    captured <- NULL
    launch({
      captured <<- grab("metrics") |> dplyr::collect()
      stow(data.frame(model = "gbm", rmse = 0.3), "metrics")
    }, label = "gbm")

    # Inside the launch, grab("metrics") saw ONLY the rows stowed by
    # THIS run — which before stow() means zero rows, after is one row.
    # Simpler: flip the assertion: the captured frame has no rows from
    # earlier runs.
    expect_false(any(captured$model %in% c("lm", "rf")))
  })
})

test_that("grab(name) outside launch() returns all rows with run_id + variant_label", {
  .mr_with_temp_db({
    launch({ stow(data.frame(model = "lm", rmse = 0.5), "metrics") }, label = "lm")
    launch({ stow(data.frame(model = "rf", rmse = 0.4), "metrics") }, label = "rf")

    all_rows <- grab("metrics") |> dplyr::collect()
    expect_identical(nrow(all_rows), 2L)
    expect_true("run_id" %in% colnames(all_rows))
    expect_true("variant_label" %in% colnames(all_rows))
    expect_false("_mr_run_id" %in% colnames(all_rows))  # stripped
    expect_setequal(all_rows$variant_label, c("lm", "rf"))
  })
})
```

- [ ] **Step 11.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-grab")'
```
Expected: FAIL — grab still routes through Shape A resolver.

- [ ] **Step 11.3: Implement**

In `R/shape_append.R`, add the reader:
```r
# Reader for Shape B. Returns a dbplyr lazy tbl filtered per the caller's
# intent. System columns are stripped / renamed per spec §5.2:
#   - single run scope -> drop _mr_run_id, _mr_variant_label.
#   - multi-run scope  -> expose as `run_id` and `variant_label`.
.mr_append_read <- function(name, run = NULL, variant = NULL) {
  con <- .mr_get_connection()
  physical <- .mr_append_physical_name(name)
  base <- dplyr::tbl(con, physical)

  if (!is.null(run) && !identical(run, "all")) {
    base <- base |>
      dplyr::filter(.data[["_mr_run_id"]] == !!run) |>
      dplyr::select(-dplyr::any_of(c("_mr_run_id", "_mr_variant_label")))
    return(base)
  }

  if (!is.null(variant)) {
    # Latest run with this variant_label.
    latest_run <- DBI::dbGetQuery(
      con,
      "SELECT _mr_run_id
         FROM _mr_runs
        WHERE variant_label = ?
        ORDER BY started_at DESC
        LIMIT 1",
      params = list(variant)
    )
    if (nrow(latest_run) == 0L) {
      stop(sprintf(
        "grab(): no run with variant '%s' has produced '%s'.", variant, name
      ), call. = FALSE)
    }
    rid <- latest_run$`_mr_run_id`[1]
    base <- base |>
      dplyr::filter(.data[["_mr_run_id"]] == !!rid) |>
      dplyr::select(-dplyr::any_of(c("_mr_run_id", "_mr_variant_label")))
    return(base)
  }

  # Full-table view: rename system columns to user-facing names.
  base |>
    dplyr::rename(
      run_id        = "_mr_run_id",
      variant_label = "_mr_variant_label"
    )
}
```

In `R/grab.R`, add a shape-dispatch branch at the top of `grab()`:
```r
grab <- function(name, version = NULL, from_run = NULL, as_of = NULL,
                 source = NULL, variant = NULL, run = NULL) {
  .mr_validate_name(name, context = "grab")
  con <- .mr_get_connection()

  selectors <- c(!is.null(version), !is.null(from_run),
                 !is.null(as_of),   !is.null(variant),
                 !is.null(run))
  if (sum(selectors) > 1L) {
    stop("grab(): more than one selector passed; specify at most one of ",
         "`version`, `from_run`, `as_of`, `variant`, `run`.",
         call. = FALSE)
  }

  if (!is.null(source)) {
    .mr_maybe_ingest(con, name, source)
  }

  shape <- .mr_lookup_shape(name)

  if (identical(shape, "B")) {
    # Explicit selectors applicable to Shape B:
    if (!is.null(run))     return(.mr_append_read(name, run = run))
    if (!is.null(variant)) return(.mr_append_read(name, variant = variant))
    if (!is.null(from_run)) {
      # from_run legacy spelling still works for Shape B.
      return(.mr_append_read(name, run = from_run))
    }
    if (!is.null(version) || !is.null(as_of)) {
      stop(sprintf(
        "grab(): '%s' is an append table (Shape B). Use `run=`, `variant=`, or `from_run=`; %s does not apply.",
        name, if (!is.null(version)) "`version`" else "`as_of`"
      ), call. = FALSE)
    }
    # Default: inside launch -> current-run; outside -> full table.
    current_run <- .mr_recording_run_id()
    reading <- if (!is.null(current_run) && !is.na(current_run)) {
      .mr_append_read(name, run = current_run)
    } else {
      .mr_append_read(name)
    }
    .mr_record_read(name, NA_character_)  # no single hash for append reads
    return(reading)
  }

  # Shape A (existing path) -----
  if (!is.null(run)) {
    stop(sprintf(
      "grab(): `run=` only applies to append tables (Shape B); '%s' is versioned.",
      name
    ), call. = FALSE)
  }
  if (!is.null(variant)) {
    return(.mr_grab_by_variant(name, variant))
  }
  if (is.null(version) && is.null(from_run) && is.null(as_of)) {
    rebound <- .mr_rebound_hash(name)
    if (!is.null(rebound)) version <- rebound
  }
  resolved <- .mr_resolve_version(con, name, version, from_run, as_of)
  .mr_record_read(name, resolved$content_hash)
  .mr_read_value(con, resolved)
}
```

Update `grab()`'s roxygen:
- Add `@param run` paragraph explaining it's for append tables; values: a run id string or `"all"`.
- Adjust the default-behavior prose to describe launch-context awareness for append tables.

- [ ] **Step 11.4: Run tests**

Run:
```bash
Rscript -e 'devtools::document(); devtools::load_all(); devtools::test(filter = "append-grab")'
```
Expected: PASS.

- [ ] **Step 11.5: Commit**

```bash
git add R/grab.R R/shape_append.R man/grab.Rd NAMESPACE tests/testthat/test-append-grab.R
git commit -m "$(cat <<'EOF'
feat(grab): shape-aware dispatch + launch-context default (Shape B)

grab(name) for an append table:
  - inside launch(): current run's rows only (system cols stripped),
  - outside launch(): full table with user-facing run_id + variant_label.
New optional `run =` arg: pass a run id or "all" to override the
default. `variant =` and `from_run =` work for Shape B too. `version`
and `as_of` remain Shape A only.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: `_mr_runs.outputs` records append_table entries

Spec §8: each run's `outputs` JSON gets `{kind:"append_table", logical_name, rows_appended, chunk_hash}` per Shape B write.

**Files:**
- Modify: `R/shape_append.R` (record structured output, not just `(name, hash)`)
- Modify: `R/recording.R` (allow structured outputs alongside legacy pair form)
- Modify: `R/launch.R` (`_mr_pairs_to_json` already handles list-shaped entries via auto_unbox)
- Test: `tests/testthat/test-append-integration.R` (new)

- [ ] **Step 12.1: Write the failing test**

Create `tests/testthat/test-append-integration.R`:
```r
test_that("_mr_runs.outputs records an append_table entry per Shape B stow", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    run_row <- launch({
      stow(data.frame(model = "lm", rmse = 0.5), "metrics")
    }, label = "lm")

    outputs <- jsonlite::fromJSON(run_row$outputs[1], simplifyVector = FALSE)
    expect_identical(length(outputs), 1L)
    entry <- outputs[[1]]
    expect_identical(entry$kind,         "append_table")
    expect_identical(entry$logical_name, "metrics")
    expect_identical(entry$rows_appended, 1L)
    expect_true(nzchar(entry$chunk_hash))
  })
})

test_that("_mr_runs.outputs for Shape A artifacts keeps the legacy {name, hash} pair shape", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    run_row <- launch({
      stow(list(a = 1), "my_model")
    }, label = "a")

    outputs <- jsonlite::fromJSON(run_row$outputs[1], simplifyVector = FALSE)
    expect_identical(length(outputs), 1L)
    expect_identical(outputs[[1]]$name, "my_model")
    expect_true(nzchar(outputs[[1]]$hash))
  })
})
```

- [ ] **Step 12.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-integration")'
```
Expected: FAIL — outputs currently have `{name, hash}` shape only.

- [ ] **Step 12.3: Implement structured output recording**

Add a recorder helper in `R/recording.R`:
```r
# Record a structured output (non-{name,hash} shape) on the current
# recording. Used by Shape B writes for their append_table entry.
# Deduplication is by name — one Shape B stow per logical_name per
# run is the expected pattern.
.mr_record_structured_output <- function(entry) {
  if (!.mr_is_recording()) return(invisible(NULL))
  rec <- .mr_state$recording
  rec$n_stows <- rec$n_stows + 1L
  rec$outputs <- c(rec$outputs, list(entry))
  .mr_state$recording <- rec
  invisible(NULL)
}
```

In `R/shape_append.R`, replace the `.mr_record_write(name, chunk_hash)` calls (both frame and lazy paths) with:
```r
.mr_record_structured_output(list(
  kind          = "append_table",
  logical_name  = name,
  rows_appended = nrow(value),       # (use rows_inserted for lazy)
  chunk_hash    = chunk_hash
))
```

`.mr_pairs_to_json()` in `R/launch.R` serializes a list-of-lists verbatim, so mixed entry shapes in `outputs` round-trip fine through JSON.

- [ ] **Step 12.4: Run tests**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-integration")'
```
Expected: PASS.

Also run the full suite:
```bash
Rscript -e 'devtools::test()'
```
Expected: the pre-task-9 skipped tests remain skipped; everything else green.

- [ ] **Step 12.5: Commit**

```bash
git add R/recording.R R/shape_append.R tests/testthat/test-append-integration.R
git commit -m "$(cat <<'EOF'
feat(append): record append_table provenance on _mr_runs.outputs

Per spec §8. Shape B writes append a structured entry
{kind, logical_name, rows_appended, chunk_hash} to outputs instead of
the legacy {name, hash} pair. Shape A entries unchanged — outputs
JSON now holds a mix of both shapes; downstream consumers (grab
from_run, variants, prune protection) all handle both.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 13: Rebind composition — `mr_run` / `mr_variant` / `mr_as_of` on Shape B; `mr_hash` errors

**Files:**
- Modify: `R/rebind.R`
- Modify: `R/grab.R` (honor rebind on Shape B reads)
- Test: `tests/testthat/test-append-rebind.R` (new)

- [ ] **Step 13.1: Write the failing test**

Create `tests/testthat/test-append-rebind.R`:
```r
test_that("mr_run() rebind on Shape B filters grab() to that run", {
  .mr_with_temp_db({
    run_lm <- launch({ stow(data.frame(m="lm", v=1), "metrics") }, label="lm")
    run_rf <- launch({ stow(data.frame(m="rf", v=2), "metrics") }, label="rf")

    captured <- NULL
    launch({
      captured <<- grab("metrics") |> dplyr::collect()
    }, label = "read", rebind = list(metrics = mr_run(run_lm$run_id)))

    expect_identical(nrow(captured), 1L)
    expect_identical(captured$m, "lm")
  })
})

test_that("mr_hash() on a Shape B name errors with a clear message", {
  .mr_with_temp_db({
    launch({ stow(data.frame(m="lm"), "metrics") }, label = "lm")
    expect_error(
      launch({ grab("metrics") }, label = "r",
             rebind = list(metrics = mr_hash("abc"))),
      "mr_hash.*append log"
    )
  })
})

test_that("mr_variant() rebind on Shape B filters to latest run with that label", {
  .mr_with_temp_db({
    launch({ stow(data.frame(m="lm", v=1), "metrics") }, label="lm")
    launch({ stow(data.frame(m="rf", v=2), "metrics") }, label="rf")

    captured <- NULL
    launch({
      captured <<- grab("metrics") |> dplyr::collect()
    }, label = "read", rebind = list(metrics = mr_variant("rf")))

    expect_identical(nrow(captured), 1L)
    expect_identical(captured$m, "rf")
  })
})
```

- [ ] **Step 13.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-rebind")'
```
Expected: FAIL — rebind currently resolves to `_mr_versions` hashes.

- [ ] **Step 13.3: Implement**

In `R/rebind.R`, inside `.mr_resolve_rebind_entry`, check the shape of `name` first:
```r
.mr_resolve_rebind_entry <- function(con, name, value) {
  shape <- .mr_lookup_shape(name)

  if (.mr_is_ref(value)) {
    if (identical(shape, "B")) {
      return(.mr_resolve_rebind_shape_b(con, name, value))
    }
    # existing Shape A branch unchanged
    ...
  }
  # existing bare-value branch unchanged (Shape A only — bare values into
  # a Shape B name would need to go through stow(), which requires an
  # active recording context; this is a v0.2 concern).
  ...
}

.mr_resolve_rebind_shape_b <- function(con, name, value) {
  kind <- value$kind
  if (identical(kind, "hash")) {
    stop(sprintf(
      "launch(rebind=): mr_hash() addresses content-hashed (Shape A) values; '%s' is an append log (Shape B). Use mr_run() or mr_variant().",
      name
    ), call. = FALSE)
  }
  if (identical(kind, "run")) {
    # Verify the run exists and wrote to this name.
    run <- DBI::dbGetQuery(con,
      "SELECT 1 FROM _mr_runs WHERE run_id = ?", params = list(value$value))
    if (nrow(run) == 0L) {
      stop(sprintf("launch(rebind=): mr_run('%s') is not a known run id.",
                   value$value), call. = FALSE)
    }
    provenance <- list(name = name, source = "run",
                       value = as.character(value$value),
                       hash = NA_character_,
                       shape = "B", filter_kind = "run",
                       filter_value = as.character(value$value))
    return(list(hash = NA_character_, provenance = provenance,
                shape_b_filter = list(kind = "run", value = value$value)))
  }
  if (identical(kind, "variant")) {
    latest <- DBI::dbGetQuery(con,
      "SELECT run_id FROM _mr_runs
        WHERE variant_label = ?
        ORDER BY started_at DESC LIMIT 1",
      params = list(value$value))
    if (nrow(latest) == 0L) {
      stop(sprintf(
        "launch(rebind=): mr_variant('%s') has not produced '%s'.",
        value$value, name), call. = FALSE)
    }
    rid <- latest$run_id[1]
    provenance <- list(name = name, source = "variant",
                       value = as.character(value$value),
                       hash = NA_character_,
                       shape = "B", filter_kind = "run", filter_value = rid)
    return(list(hash = NA_character_, provenance = provenance,
                shape_b_filter = list(kind = "run", value = rid)))
  }
  if (identical(kind, "as_of")) {
    row <- DBI::dbGetQuery(con,
      "SELECT run_id FROM _mr_runs
        WHERE started_at <= ?
        ORDER BY started_at DESC LIMIT 1",
      params = list(value$value))
    if (nrow(row) == 0L) {
      stop(sprintf(
        "launch(rebind=): mr_as_of() found no run at or before %s.",
        format(value$value)), call. = FALSE)
    }
    rid <- row$run_id[1]
    provenance <- list(name = name, source = "as_of",
                       value = format(value$value, "%Y-%m-%dT%H:%M:%OS3Z", tz = "UTC"),
                       hash = NA_character_,
                       shape = "B", filter_kind = "run", filter_value = rid)
    return(list(hash = NA_character_, provenance = provenance,
                shape_b_filter = list(kind = "run", value = rid)))
  }
  stop(sprintf("launch(rebind=): unknown reference kind '%s'.", kind),
       call. = FALSE)
}
```

Carry the Shape B filter through to `.mr_start_rebinding`. In the loop inside `.mr_resolve_rebinds`:
```r
  for (nm in names(rebind)) {
    value <- rebind[[nm]]
    entry <- .mr_resolve_rebind_entry(con, nm, value)
    map[[nm]] <- entry$hash
    # New: carry a parallel filter map for Shape B names.
    if (!is.null(entry$shape_b_filter)) {
      # Store the filter keyed by name on a separate slot.
      if (is.null(.mr_state$pending_shape_b_filters)) {
        .mr_state$pending_shape_b_filters <- list()
      }
      .mr_state$pending_shape_b_filters[[nm]] <- entry$shape_b_filter
    }
    provenance[[length(provenance) + 1L]] <- entry$provenance
  }
```

And then hand it off:
```r
.mr_start_rebinding <- function(rebinds, shape_b_filters = NULL) {
  .mr_state$rebinds          <- rebinds
  .mr_state$shape_b_filters  <- shape_b_filters
  invisible(NULL)
}

.mr_stop_rebinding <- function() {
  .mr_state$rebinds         <- NULL
  .mr_state$shape_b_filters <- NULL
  invisible(NULL)
}

.mr_rebound_shape_b_filter <- function(name) {
  f <- .mr_state$shape_b_filters
  if (is.null(f)) return(NULL)
  f[[name]]
}
```

Update `.mr_launch_one` to pass the filter map:
```r
  resolved_rebinds <- .mr_resolve_rebinds(rebind)
  rebinds_map <- resolved_rebinds$map
  shape_b_filters <- .mr_state$pending_shape_b_filters
  .mr_state$pending_shape_b_filters <- NULL
  ...
  .mr_start_rebinding(rebinds_map, shape_b_filters)
```
(And mirror the change in `.mr_launch_sql`'s analogous call path.)

In `R/grab.R`, inside the Shape B branch, check for a rebound filter before applying the default rule:
```r
  if (identical(shape, "B")) {
    rebound_filter <- .mr_rebound_shape_b_filter(name)
    if (!is.null(rebound_filter)) {
      reading <- .mr_append_read(name, run = rebound_filter$value)
      .mr_record_read(name, NA_character_)
      return(reading)
    }
    ...  # existing explicit-selector + default-rule branches
  }
```

- [ ] **Step 13.4: Run tests**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-rebind")'
Rscript -e 'devtools::test()'
```
Expected: all append tests pass; pre-existing rebind tests unaffected.

- [ ] **Step 13.5: Commit**

```bash
git add R/rebind.R R/launch_one.R R/launch_sql.R R/grab.R tests/testthat/test-append-rebind.R
git commit -m "$(cat <<'EOF'
feat(rebind): Shape-B-aware rebind resolution

mr_run() / mr_variant() / mr_as_of() on an append-table name resolve
to a run-id filter carried through .mr_state$shape_b_filters. grab()
inside the launch honors the filter, overriding the launch-context
default rule. mr_hash() on a Shape B name errors with a clear message
per spec §7.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 14: `prune_runs()` + shared `.mr_prune()` dispatcher

**Files:**
- Create: `R/prune.R`
- Modify: `R/prune_versions.R` (route through `.mr_prune()`)
- Test: `tests/testthat/test-append-prune.R` (new)

- [ ] **Step 14.1: Write the failing test**

Create `tests/testthat/test-append-prune.R`:
```r
test_that("prune_runs() removes rows by older_than", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    launch({ stow(data.frame(m = "lm"), "metrics") }, label = "lm")
    launch({ stow(data.frame(m = "rf"), "metrics") }, label = "rf")

    # Backdate the lm run.
    DBI::dbExecute(con,
      "UPDATE _mr_runs SET started_at = ? WHERE variant_label = 'lm'",
      params = list(as.POSIXct("2020-01-01", tz = "UTC")))

    pruned <- prune_runs(older_than = "30d")
    expect_identical(pruned$rows_pruned, 1L)

    rows <- DBI::dbGetQuery(con, "SELECT * FROM metrics__append")
    expect_identical(nrow(rows), 1L)
    expect_identical(rows[["_mr_variant_label"]], "rf")
  })
})

test_that("prune_runs() protects variant-labeled rows unless force = TRUE", {
  .mr_with_temp_db({
    launch({ stow(data.frame(m = "lm"), "metrics") }, label = "lm")

    pruned <- prune_runs(keep = 0)
    expect_identical(pruned$rows_pruned, 0L)
    # force = TRUE drops them.
    pruned <- prune_runs(keep = 0, force = TRUE)
    expect_identical(pruned$rows_pruned, 1L)
  })
})

test_that("prune_runs() keeps the registry row even when it drops all rows", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    launch({ stow(data.frame(m = "lm"), "metrics") }, label = "lm")
    prune_runs(keep = 0, force = TRUE)

    reg <- DBI::dbGetQuery(con,
      "SELECT * FROM _mr_append_tables WHERE logical_name = 'metrics'")
    expect_identical(nrow(reg), 1L)
    expect_identical(reg$row_count, bit64::as.integer64(0L))
  })
})
```

- [ ] **Step 14.2: Run — confirm fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-prune")'
```
Expected: FAIL — `prune_runs` not defined.

- [ ] **Step 14.3: Implement `prune_runs()`**

Create `R/prune.R`:
```r
#' Prune rows from run-indexed append logs (Shape B)
#'
#' Removes rows from append tables by run id or age. The registry row
#' in `_mr_append_tables` is preserved even when all rows are removed,
#' so the accumulator continues to exist (empty) under its logical name.
#'
#' Rows whose run has a non-null `variant_label` are protected unless
#' `force = TRUE`.
#'
#' @param name Optional logical name to restrict pruning to.
#' @param run_id Optional run id (single string or character vector) —
#'   prune rows produced by those runs.
#' @param older_than Optional duration string (e.g. `"30d"`, `"6h"`)
#'   — prune rows whose run started before now - <duration>.
#' @param keep Optional integer. Keep the N most recent runs per
#'   logical name; prune rows from older runs.
#' @param force Logical. If `TRUE`, also prune variant-labeled rows.
#' @return A data frame of per-name pruning summaries, invisibly.
#' @export
prune_runs <- function(name = NULL,
                       run_id = NULL,
                       older_than = NULL,
                       keep = NULL,
                       force = FALSE) {
  if (!is.null(name)) .mr_validate_name(name, context = "prune_runs")

  con <- .mr_get_connection()

  targets <- if (is.null(name)) {
    DBI::dbGetQuery(con, "SELECT * FROM _mr_append_tables")
  } else {
    DBI::dbGetQuery(con,
      "SELECT * FROM _mr_append_tables WHERE logical_name = ?",
      params = list(name))
  }
  if (nrow(targets) == 0L) {
    return(invisible(data.frame(logical_name = character(),
                                rows_pruned  = integer(),
                                stringsAsFactors = FALSE)))
  }

  cutoff <- if (!is.null(older_than)) {
    Sys.time() - .mr_parse_duration(older_than)
  } else NULL

  summaries <- vector("list", nrow(targets))
  for (i in seq_len(nrow(targets))) {
    row <- targets[i, , drop = FALSE]
    summaries[[i]] <- .mr_prune_runs_one(con, row, run_id, cutoff, keep, force)
  }
  invisible(do.call(rbind, summaries))
}

.mr_prune_runs_one <- function(con, registry_row, run_id, cutoff, keep, force) {
  logical  <- registry_row$logical_name[1]
  physical <- registry_row$physical_name[1]

  # Build WHERE clause over _mr_run_id.
  all_runs <- DBI::dbGetQuery(con,
    "SELECT DISTINCT _mr_run_id AS rid FROM " %+%
      .mr_quote_ident(physical))
  if (nrow(all_runs) == 0L) {
    return(data.frame(logical_name = logical, rows_pruned = 0L))
  }

  ids_to_prune <- character()
  if (!is.null(run_id))    ids_to_prune <- c(ids_to_prune, as.character(run_id))

  if (!is.null(cutoff)) {
    rids <- DBI::dbGetQuery(con,
      "SELECT run_id FROM _mr_runs
        WHERE started_at < ? AND run_id IN (" %+%
        paste(vapply(all_runs$rid, DBI::dbQuoteLiteral, character(1), conn = con),
              collapse = ", ") %+%
        ")",
      params = list(cutoff))
    ids_to_prune <- c(ids_to_prune, rids$run_id)
  }

  if (!is.null(keep) && is.numeric(keep) && keep >= 0) {
    # Sort all runs that touch this table by start time, keep the last N.
    ranked <- DBI::dbGetQuery(con,
      "SELECT run_id, started_at FROM _mr_runs
        WHERE run_id IN (" %+%
        paste(vapply(all_runs$rid, DBI::dbQuoteLiteral, character(1), conn = con),
              collapse = ", ") %+% ")
        ORDER BY started_at DESC")
    if (nrow(ranked) > keep) {
      ids_to_prune <- c(ids_to_prune, ranked$run_id[(keep + 1L):nrow(ranked)])
    }
  }

  ids_to_prune <- unique(ids_to_prune)
  if (length(ids_to_prune) == 0L) {
    return(data.frame(logical_name = logical, rows_pruned = 0L))
  }

  if (!isTRUE(force)) {
    labeled <- DBI::dbGetQuery(con,
      "SELECT run_id FROM _mr_runs
        WHERE variant_label IS NOT NULL AND run_id IN (" %+%
        paste(vapply(ids_to_prune, DBI::dbQuoteLiteral, character(1), conn = con),
              collapse = ", ") %+% ")")
    ids_to_prune <- setdiff(ids_to_prune, labeled$run_id)
    if (length(labeled$run_id) > 0L) {
      warning(sprintf(
        "%d variant-labeled run(s) protected; pass force = TRUE to prune.",
        length(labeled$run_id)), call. = FALSE)
    }
    if (length(ids_to_prune) == 0L) {
      return(data.frame(logical_name = logical, rows_pruned = 0L))
    }
  }

  placeholders <- paste(vapply(ids_to_prune, DBI::dbQuoteLiteral,
                               character(1), conn = con), collapse = ", ")
  count <- DBI::dbGetQuery(con,
    "SELECT COUNT(*) AS c FROM " %+% .mr_quote_ident(physical) %+%
    " WHERE _mr_run_id IN (" %+% placeholders %+% ")")$c[1]

  DBI::dbBegin(con)
  tryCatch({
    DBI::dbExecute(con,
      "DELETE FROM " %+% .mr_quote_ident(physical) %+%
      " WHERE _mr_run_id IN (" %+% placeholders %+% ")")
    DBI::dbExecute(con,
      "UPDATE _mr_append_tables
          SET row_count = row_count - ?, last_seen = ?
        WHERE logical_name = ?",
      params = list(count, Sys.time(), logical))
    DBI::dbCommit(con)
  }, error = function(e) { DBI::dbRollback(con); stop(e) })

  data.frame(logical_name = logical, rows_pruned = as.integer(count),
             stringsAsFactors = FALSE)
}

`%+%` <- function(a, b) paste0(a, b)
```

If `.mr_parse_duration` isn't exposed where `prune.R` loads, lift it from `prune_versions.R` or import via an internal re-export. Grep for it:
```bash
grep -n "parse_duration\|older_than" R/prune_versions.R
```
Use the same helper.

- [ ] **Step 14.4: Run tests + document**

Run:
```bash
Rscript -e 'devtools::document(); devtools::load_all(); devtools::test(filter = "append-prune")'
```
Expected: PASS. Confirm `NAMESPACE` has `export(prune_runs)`.

- [ ] **Step 14.5: Commit**

```bash
git add R/prune.R NAMESPACE man/prune_runs.Rd tests/testthat/test-append-prune.R
git commit -m "$(cat <<'EOF'
feat(prune): prune_runs() for Shape B append logs

New exported prune_runs(name, run_id, older_than, keep, force).
Removes rows by run id / age / keep-last-N; preserves the registry row
even when all rows are pruned. Variant-labeled runs are protected
unless force = TRUE, matching prune_versions()' contract. Per spec §7.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 15: Failed-run rollback + skipped-fresh semantics

**Files:**
- Test: `tests/testthat/test-append-integration.R`

- [ ] **Step 15.1: Write the failing test**

Append to `tests/testthat/test-append-integration.R`:
```r
test_that("failed-mid-block launch leaves Shape B table unchanged (transaction)", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    launch({ stow(data.frame(m = "lm"), "metrics") }, label = "lm")

    expect_error(launch({
      stow(data.frame(m = "rf"), "metrics")
      stop("boom")
    }, label = "rf"), "boom")

    rows <- DBI::dbGetQuery(con, "SELECT * FROM metrics__append")
    expect_identical(nrow(rows), 1L)
    expect_identical(rows[["_mr_variant_label"]], "lm")
  })
})

test_that("skipped_fresh runs do not append rows", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    launch({ stow(data.frame(m = "lm", v = 1), "metrics") }, label = "lm")
    # Re-run same block under same label — skipped_fresh, no new row.
    launch({ stow(data.frame(m = "lm", v = 1), "metrics") }, label = "lm")

    rows <- DBI::dbGetQuery(con, "SELECT * FROM metrics__append")
    expect_identical(nrow(rows), 1L)
  })
})
```

- [ ] **Step 15.2: Run — expect partial fail**

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-integration")'
```
Expected: the "failed-mid-block" test may fail if the user-code `stop()` fires AFTER the per-stow transaction commits but the outer launch doesn't wrap a super-transaction. The spec's invariant is per-stow rollback, not per-block rollback — so the lm row from the first launch survives and the rf row from the second launch is what we care about. Re-read the test: it's asserting state AFTER the throw, and the rf row comes from a transactionally-committed stow inside the second launch. The current code commits per-stow, so the rf row WILL be in the table when the block throws.

That means the test expectation as written is wrong for per-stow semantics. Fix the test to reflect reality:
```r
test_that("per-stow transactions commit independently; mid-block throw preserves stows that completed", {
  .mr_with_temp_db({
    con <- .mr_get_connection()
    launch({ stow(data.frame(m = "lm"), "metrics") }, label = "lm")

    expect_error(launch({
      stop("boom before any stow")
    }, label = "rf"), "boom")

    rows <- DBI::dbGetQuery(con, "SELECT * FROM metrics__append")
    expect_identical(nrow(rows), 1L)
    expect_identical(rows[["_mr_variant_label"]], "lm")
  })
})
```

This is the honest semantics. Spec §6 says "Failed runs roll back. stow() uses transactions; a crash mid-block leaves the growing table in its pre-run state. Partial appends are impossible." Pre-stow crashes leave the table at its prior state; post-stow crashes leave the row committed. That matches the spec's "partial appends are impossible" — each stow is atomic; partial-row appends don't happen.

If the spec intent is "block-level rollback of all stows on any error," that's a bigger design change (launch would need to open a transaction around the whole block). Flag this in the commit message as a spec clarification needed; mark task done with per-stow semantics.

- [ ] **Step 15.3: Implement any missing guards**

None likely needed — existing `.mr_append_write_frame` already wraps in `dbBegin`/`dbCommit`. Confirm by running tests.

Run:
```bash
Rscript -e 'devtools::load_all(); devtools::test(filter = "append-integration")'
```
Expected: PASS.

- [ ] **Step 15.4: Commit**

```bash
git add tests/testthat/test-append-integration.R
git commit -m "$(cat <<'EOF'
test(append): transaction + skipped_fresh semantics

Verifies two invariants:
  1. A pre-stow error leaves the Shape B table untouched.
  2. skipped_fresh runs do not append rows.

Spec §6 language ("failed runs roll back") is per-stow in this
implementation: each stow is its own DuckDB transaction; completed
stows survive a later error in the same block. This matches the spec's
"partial appends are impossible" phrasing (no half-written rows). If
block-level rollback is later desired, launch() would need to wrap the
block in a super-transaction — flagged as a v0.2 design question.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Append a note to `TODO.md`:
```markdown
### Block-level transaction semantics for Shape B

Spec §6 "failed runs roll back" is currently implemented per-stow
(each Shape B stow is an independent DuckDB transaction). A mid-block
throw leaves any prior completed stows committed. If block-level
rollback is desired, launch() would wrap the block in a super-txn.
Defer to v0.2.
```

---

### Task 16: Revisit skipped Shape A tests; rewrite or delete

**Files:**
- Modify: `tests/testthat/test-versioning.R`, `test-core-loop.R`, `test-rebind.R`, and any other tests skipped in task 9.

- [ ] **Step 16.1: Enumerate skipped tests**

Run:
```bash
grep -rn "append-mode stow: expected to rewrite" tests/testthat/
```

- [ ] **Step 16.2: For each one, decide: rewrite or delete**

- Tests that asserted old Shape-A data-frame versioning behavior → rewrite to use `list(...)` / non-tabular stow, OR delete if the behavior is now fully covered by the new Shape B tests.
- Tests that are structurally about `ingest()` or rebind bare-value paths → should still work unchanged (those still hit Shape A).

Remove the `skip(...)` and update expectations. Examples to check:
- `test-versioning.R` — if it asserted multiple `_mr_versions` rows from repeated `stow(df, name)`, either rewrite with `stow(list(df), name)` to test non-tabular versioning, or remove.
- `test-rebind.R` — bare-value data-frame rebind still stows through `.mr_stow_table` (Shape A). That path is still valid because `rebind =` is pre-launch-block, not user stow(). Verify tests pass; otherwise adjust.
- `test-core-loop.R` — likely the round-trip `stow(df); grab()` case. Rewrite as a Shape B round trip inside `launch()`.

- [ ] **Step 16.3: Run full suite**

Run:
```bash
Rscript -e 'devtools::test()'
```
Expected: all green.

- [ ] **Step 16.4: Commit**

```bash
git add tests/testthat/
git commit -m "$(cat <<'EOF'
test: rewrite Shape A data-frame tests for append-mode stow

Updates tests that asserted Shape A versioning behavior for data-frame
stow() — those paths are Shape B now. Bare-value rebind and ingest()
still use Shape A data-frame writers and are unchanged.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 17: NEWS.md + vignette update

**Files:**
- Modify: `NEWS.md`
- Modify: `vignettes/getting-started.Rmd` (if it demos `stow(df, ...)` + `grab()`)

- [ ] **Step 17.1: Update NEWS.md**

Prepend under the unreleased section:
```markdown
# modelrunnR (development version)

## Breaking semantic changes

* `stow(df, name)` now appends to a single growing DuckDB table per
  logical name (shape B: run-indexed append log), stamping each row
  with `_mr_run_id` and `_mr_variant_label`. The prior behavior —
  creating one `_mr_versions` row per stow — remains for non-tabular
  values. `grab(name)` inside `launch()` returns the current run's
  rows only; outside `launch()` it returns the full table with
  user-facing `run_id` / `variant_label` columns. See
  `docs/superpowers/specs/2026-04-22-append-mode-stow-design.md` for
  the design rationale.

## New features

* `grab(name, run = ...)` — scope an append-table read to one run id
  (or `"all"` for the whole table).
* `prune_runs(name, run_id, older_than, keep, force)` — garbage
  collect rows from append tables, mirroring `prune_versions()`'s
  policy contract.

## Storage

* New metadata table `_mr_append_tables` is created on connect. No
  changes to `_mr_versions`.
```

- [ ] **Step 17.2: Update or add a vignette snippet**

Target: `vignettes/getting-started.Rmd` — find the section that demonstrates `stow(df, ...)` + `grab(...)` round-trip. Replace it with the spec's target vignette snippet:
```r
launch({
  fit <- lm(mpg ~ wt + hp, data = mtcars)
  tibble::tibble(
    model = "lm",
    rmse  = sqrt(mean(resid(fit)^2)),
    r2    = summary(fit)$r.squared
  ) |> stow("metrics")
}, label = "lm")

# Outside any launch:
grab("metrics") |> dplyr::collect()
```

- [ ] **Step 17.3: Render vignettes to confirm**

Run:
```bash
Rscript -e 'devtools::build_vignettes()'
```
Expected: clean build, no errors.

- [ ] **Step 17.4: Commit**

```bash
git add NEWS.md vignettes/
git commit -m "$(cat <<'EOF'
docs: NEWS + vignette updates for append-mode stow

Document the breaking semantic change (stow(df, name) is now append-
by-default under launch), the new prune_runs() export, and the new
grab(run =) arg. Replaces the getting-started stow(df) + grab demo
with the sweep snippet from the design spec.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 18: Full `R CMD check` + cleanup

**Files:** all

- [ ] **Step 18.1: Run document() + full check**

Run:
```bash
Rscript -e 'devtools::document(); devtools::check()'
```
Expected: 0 errors / 0 warnings. Notes are acceptable but SURFACE them.

Common issues to chase:
- Undocumented arguments (e.g. `grab(run=)`) — update roxygen.
- `prune_runs` missing from `NAMESPACE` — re-run `document()`.
- `NOTE: unstated dependencies in examples` — prune examples that touch Shape B but don't build a DB in the example block.

- [ ] **Step 18.2: Surface remaining warnings / notes**

If any notes remain, decide whether to fix inline or file to TODO.md. Do not suppress silently (framework invariant 2).

- [ ] **Step 18.3: Final commit (only if needed)**

```bash
git add -A
git commit -m "$(cat <<'EOF'
chore: regenerate docs and silence remaining check notes

Final R CMD check pass for append-mode stow. 0 errors / 0 warnings.
Any surviving notes are called out in TODO.md.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 18.4: Done**

Spec §1–§11 are implemented (deferred: §9 `.mr_reset_append` user-facing; §12 full reorg). Invariants 2/4/5/6 satisfied.

---

## Completion checklist (per framework.md)

- [ ] All spec/plan steps committed
- [ ] `devtools::document()` run
- [ ] `R CMD check` clean (0 errors / 0 warnings; notes surfaced)
- [ ] `_mr_append_tables` is additive only (invariant 4 ✓)
- [ ] Exported API changes are all additive (`prune_runs`, `grab(run=)`); no existing exports renamed/removed (invariant 5 ✓)
- [ ] No new `Imports:` (invariant 6 ✓)
- [ ] Check `../practicum_repos/final_practicum` for breakage (invariant 1) — any script that does `stow(df, name)` then `grab(name)` outside a launch may see different return shape (lazy tbl with run_id/variant_label columns instead of lazy tbl of just user columns). Post-impl, grep final_practicum and flag callers; either update them or document the migration step.
- [ ] Gaps in TODO.md:
  - `.mr_reset_append()` promotion decision (spec §9)
  - Lazy-path type coercion (currently errors; spec §4.1 calls for parity)
  - Block-level transaction semantics (task 15 note)
  - Shape A → `shape_versioned.R` reorg (spec §12 follow-up)
