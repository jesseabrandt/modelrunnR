# Inspect index

This directory holds AI-independent, human-inspectable review artifacts for this project. See `explainers/getting-started.md` for orientation. `structure.md` is the map; `code/` mirrors the source tree; `review-requests.md` collects agent-raised review signals.
