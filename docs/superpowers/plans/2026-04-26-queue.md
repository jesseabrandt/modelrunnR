# `queue()` and `launch(mr_run(id))` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a new `queue()` verb that registers a run to `_mr_runs` with `status = "queued"` without executing it, and add `launch(mr_run(id))` as the pickup point that executes queued rows in place (or relaunches non-queued rows by id, matching today's `launch(mr_label(...))` behavior).

**Architecture:** Two phases in one plan. Phase 1 implements `launch(mr_run(id))` (relaunch by run id) — currently rejected at `R/launch.R:251–258`. Phase 2 adds `queue()` (writes a `"queued"` row, captures code+rebinds, no execution) and extends Phase 1's pickup path to detect queued rows and update them in place. `_mr_runs` gains no new columns — `"queued"` is a new value of the existing `status` enum.

**Tech Stack:** R, S3 dispatch on `mr_ref` objects, DuckDB via existing `.mr_*` helpers, testthat 3, roxygen2.

**Specs:**
- `docs/superpowers/specs/2026-04-26-launch-by-run-id-design.md` (Phase 1)
- `docs/superpowers/specs/2026-04-26-queue-design.md` (Phase 2)

**Sequencing note:** Must be implemented in order — Phase 2's pickup story is `launch(mr_run(id))`, which Phase 1 builds.

---

## File Structure

**Created:**
- `R/queue.R` — `queue()` exported verb + dispatch.
- `R/launch_queued.R` — `.mr_pickup_queued_run()` internal: in-place row update on pickup.
- `tests/testthat/test-launch-by-run-id.R` — Phase 1 coverage.
- `tests/testthat/test-queue.R` — Phase 2 coverage (single, batch, dispatch errors).
- `tests/testthat/test-queue-pickup.R` — Phase 2 coverage of `launch(mr_run(id))` against queued rows.

**Modified:**
- `R/launch.R` — extend the first-arg ref dispatch to accept `mr_run()` (Phase 1); branch on resolved row's `status` to route queued-row pickup through `.mr_pickup_queued_run()` (Phase 2). Add a new internal `.mr_resolve_relaunch_run_id()` resolver (Phase 1).
- `R/launch_one.R` — extract a private helper `.mr_write_queued_row()` if needed; otherwise no change (queue uses `.mr_write_run_row()` with NA fields).
- `R/is_stale.R` — exclude `status = "queued"` rows from the "last run" lookups so they don't shadow real success rows when computing freshness.
- `R/runs.R` — verify queued rows surface with no filtering changes (likely just an integration test).
- `NAMESPACE` and `man/*.Rd` — regenerated by `devtools::document()`.
- `NEWS.md` — entries for `queue()` and `launch(mr_run(id))`, and the new `"queued"` status value.
- `vignettes/getting-started.Rmd` — short staging-workflow section.

---

# Phase 1 — `launch(mr_run(id))`

Spec: `docs/superpowers/specs/2026-04-26-launch-by-run-id-design.md`. Five tasks. Phase 1 must complete (all tests green, R CMD check clean) before Phase 2 starts.

## Task 1: Resolver — `.mr_resolve_relaunch_run_id(run_id)`

**Files:**
- Modify: `R/launch.R` (add new internal next to `.mr_resolve_relaunch()` at line 372)
- Test: `tests/testthat/test-launch-by-run-id.R`

- [ ] **Step 1: Write the failing test**

```r
# tests/testthat/test-launch-by-run-id.R

test_that(".mr_resolve_relaunch_run_id() returns step + code_body + parsed expr for inline rows", {
  withr::with_tempdir({
    .mr_with_test_db({
      r <- launch({ x <- 1 + 1 })
      resolved <- modelrunnR:::.mr_resolve_relaunch_run_id(r$run_id)
      expect_match(resolved$step, "^<inline:")
      expect_true(nzchar(resolved$code_body))
      expect_true(inherits(resolved$expr, "expression"))
    })
  })
})

test_that(".mr_resolve_relaunch_run_id() returns step + code_body + NULL expr for file rows when file present", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines("x <- 99", "fit.R")
      r <- launch("fit.R")
      resolved <- modelrunnR:::.mr_resolve_relaunch_run_id(r$run_id)
      expect_equal(normalizePath(resolved$step), normalizePath("fit.R"))
      expect_match(resolved$code_body, "x <- 99")
      expect_null(resolved$expr)
    })
  })
})

test_that(".mr_resolve_relaunch_run_id() falls back to stored snapshot when file is gone", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines("x <- 99", "fit.R")
      r <- launch("fit.R")
      file.remove("fit.R")
      expect_message(
        resolved <- modelrunnR:::.mr_resolve_relaunch_run_id(r$run_id),
        "is gone from disk"
      )
      expect_match(resolved$code_body, "x <- 99")
      expect_true(inherits(resolved$expr, "expression"))
    })
  })
})

test_that(".mr_resolve_relaunch_run_id() errors clearly when no row matches", {
  withr::with_tempdir({
    .mr_with_test_db({
      expect_error(
        modelrunnR:::.mr_resolve_relaunch_run_id("run_does_not_exist"),
        "no run with run_id"
      )
    })
  })
})

test_that(".mr_resolve_relaunch_run_id() errors when row's step is a synthetic non-launch tag", {
  withr::with_tempdir({
    .mr_with_test_db({
      con <- modelrunnR:::.mr_get_connection()
      DBI::dbExecute(con, "INSERT INTO _mr_runs (step, run_id, status, code_body) VALUES (?, ?, ?, ?)",
                     params = list("<interactive:foo>", "run_synth", "interactive", "x <- 1"))
      expect_error(
        modelrunnR:::.mr_resolve_relaunch_run_id("run_synth"),
        "synthetic"
      )
    })
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "launch-by-run-id")'
```
Expected: FAIL with `could not find function ".mr_resolve_relaunch_run_id"`.

- [ ] **Step 3: Write minimal implementation**

In `R/launch.R`, add immediately after `.mr_resolve_relaunch()` (current end ~line 418):

```r
# Resolve a run by run_id for relaunch. Mirrors .mr_resolve_relaunch()
# (label-based) but keys on run_id. See spec
# docs/superpowers/specs/2026-04-26-launch-by-run-id-design.md.
.mr_resolve_relaunch_run_id <- function(run_id) {
  con <- .mr_get_connection()
  prior <- DBI::dbGetQuery(
    con,
    "SELECT step, code_body, variant_label, status FROM _mr_runs
      WHERE run_id = ?",
    params = list(run_id)
  )
  if (nrow(prior) == 0L) {
    stop(sprintf("launch(): no run with run_id '%s'.", run_id), call. = FALSE)
  }
  step        <- prior$step[1]
  stored_body <- prior$code_body[1]
  status      <- prior$status[1]
  variant     <- prior$variant_label[1]

  # Synthetic non-launch steps (e.g. "<interactive:...>"). Mirrors the
  # rule .mr_resolve_relaunch_code() applies for inline tags.
  if (startsWith(step, "<") && !startsWith(step, "<inline:")) {
    stop(sprintf(
      "launch(): run_id '%s' refers to a synthetic step ('%s') that isn't relaunchable.",
      run_id, step
    ), call. = FALSE)
  }

  if (startsWith(step, "<inline:")) {
    if (is.na(stored_body) || !nzchar(stored_body)) {
      stop(sprintf(
        "launch(): run_id '%s' is an inline run with no stored code body.",
        run_id
      ), call. = FALSE)
    }
    return(list(step = step, code_body = stored_body,
                expr = parse(text = stored_body),
                variant_label = variant, status = status))
  }

  if (file.exists(step)) {
    file_body <- paste(readLines(step, warn = FALSE), collapse = "\n")
    return(list(step = step, code_body = file_body, expr = NULL,
                variant_label = variant, status = status))
  }
  if (!is.na(stored_body) && nzchar(stored_body)) {
    message(sprintf(
      "launch(): script '%s' is gone from disk; running the stored snapshot from run_id '%s'.",
      step, run_id
    ))
    return(list(step = step, code_body = stored_body,
                expr = parse(text = stored_body),
                variant_label = variant, status = status))
  }
  stop(sprintf(
    "launch(): script '%s' is gone and no snapshot is stored for run_id '%s'.",
    step, run_id
  ), call. = FALSE)
}
```

- [ ] **Step 4: Run tests to verify pass**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "launch-by-run-id")'
```
Expected: all 5 tests pass.

- [ ] **Step 5: Commit**

```bash
git add R/launch.R tests/testthat/test-launch-by-run-id.R
git commit -m "feat(launch): add .mr_resolve_relaunch_run_id() resolver"
```

---

## Task 2: Wire `mr_run()` through `launch()` dispatch

**Files:**
- Modify: `R/launch.R:251-259`
- Test: `tests/testthat/test-launch-by-run-id.R`

- [ ] **Step 1: Write the failing test**

```r
# Append to tests/testthat/test-launch-by-run-id.R

test_that("launch(mr_run(id)) re-executes inline pipelines", {
  withr::with_tempdir({
    .mr_with_test_db({
      r1 <- launch({ x <- 21 + 21; stow(x, "answer") })
      r2 <- launch(mr_run(r1$run_id))
      expect_equal(r2$status, "success")
      expect_match(r2$step, "^<inline:")
      # Same step (same expression hash); different run_id.
      expect_equal(r2$step, r1$step)
      expect_false(r2$run_id == r1$run_id)
    })
  })
})

test_that("launch(mr_run(id)) re-sources the file for file pipelines", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines("y <- 7; stow(y, 'seven')", "f.R")
      r1 <- launch("f.R")
      writeLines("y <- 8; stow(y, 'eight')", "f.R")  # edit the file
      r2 <- launch(mr_run(r1$run_id))
      expect_equal(r2$status, "success")
      # Re-sources from disk: the new value should be present.
      con <- modelrunnR:::.mr_get_connection()
      latest <- DBI::dbGetQuery(con,
        "SELECT logical_name FROM _mr_versions WHERE logical_name = 'eight'")
      expect_true(nrow(latest) >= 1)
    })
  })
})

test_that("launch(mr_run(id)) errors when no row matches", {
  withr::with_tempdir({
    .mr_with_test_db({
      expect_error(launch(mr_run("run_no_such")), "no run with run_id")
    })
  })
})

test_that("launch(mr_run(id)) auto-inherits the source row's variant_label when caller passes none", {
  withr::with_tempdir({
    .mr_with_test_db({
      r1 <- launch({ z <- 1 }, label = "exp_a")
      r2 <- launch(mr_run(r1$run_id))
      expect_equal(r2$variant_label, "exp_a")
    })
  })
})

test_that("launch(mr_run(id)) lets caller override variant_label", {
  withr::with_tempdir({
    .mr_with_test_db({
      r1 <- launch({ z <- 1 }, label = "exp_a")
      r2 <- launch(mr_run(r1$run_id), label = "exp_b")
      expect_equal(r2$variant_label, "exp_b")
    })
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "launch-by-run-id")'
```
Expected: FAIL with `only mr_label() is accepted as a first argument reference`.

- [ ] **Step 3: Modify dispatch in `R/launch.R`**

Replace the `R/launch.R:251-259` block:

```r
  if (!inline_mode && .mr_is_ref(code)) {
    if (!identical(code$kind, "label")) {
      stop(sprintf(
        "launch(): only mr_label() is accepted as a first argument reference; got mr_%s().",
        code$kind
      ), call. = FALSE)
    }
    relaunch_mode <- TRUE
  }
```

with:

```r
  relaunch_kind <- NULL  # "label" or "run" when relaunch_mode is TRUE
  if (!inline_mode && .mr_is_ref(code)) {
    if (!(code$kind %in% c("label", "run"))) {
      stop(sprintf(
        "launch(): only mr_label() and mr_run() are accepted as first argument references; got mr_%s().",
        code$kind
      ), call. = FALSE)
    }
    relaunch_mode <- TRUE
    relaunch_kind <- code$kind
  }
```

Then update the `relaunch_mode` branch in the resolution block (currently at `R/launch.R:268-274`) to dispatch on `relaunch_kind`:

Replace:
```r
  } else if (relaunch_mode) {
    resolved <- .mr_resolve_relaunch(code$value)
    step          <- resolved$step
    code_body     <- resolved$code_body
    relaunch_expr <- resolved$expr
    # Auto-inherit the label unless the user passed one explicitly.
    if (is.na(label)) label <- code$value
  } else {
```

with:
```r
  } else if (relaunch_mode) {
    resolved <- if (identical(relaunch_kind, "label")) {
      .mr_resolve_relaunch(code$value)
    } else {
      .mr_resolve_relaunch_run_id(code$value)
    }
    step          <- resolved$step
    code_body     <- resolved$code_body
    relaunch_expr <- resolved$expr
    # Auto-inherit label. For mr_label() the label is the ref's value.
    # For mr_run() it's the source row's variant_label (may be NA).
    if (is.na(label)) {
      label <- if (identical(relaunch_kind, "label")) {
        code$value
      } else {
        resolved$variant_label
      }
    }
  } else {
```

- [ ] **Step 4: Run tests to verify pass**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "launch-by-run-id")'
```
Expected: all Task 2 tests pass; Task 1 tests still pass.

- [ ] **Step 5: Run full test suite to catch regressions in mr_label() relaunch**

```
Rscript -e 'devtools::load_all(); devtools::test()'
```
Expected: all green.

- [ ] **Step 6: Commit**

```bash
git add R/launch.R tests/testthat/test-launch-by-run-id.R
git commit -m "feat(launch): accept mr_run() as first-arg reference (relaunch by run id)"
```

---

## Task 3: Warn on relaunching a non-success row (with option)

**Files:**
- Modify: `R/launch.R` (after the resolution block, before `.mr_launch_one()` call ~line 312)
- Test: `tests/testthat/test-launch-by-run-id.R`

- [ ] **Step 1: Write the failing test**

```r
# Append to tests/testthat/test-launch-by-run-id.R

test_that("launch(mr_run(id)) warns by default when source row's status isn't success", {
  withr::with_tempdir({
    .mr_with_test_db({
      r1 <- tryCatch(launch({ stop("boom") }), error = function(e) NULL)
      con <- modelrunnR:::.mr_get_connection()
      bad_id <- DBI::dbGetQuery(con, "SELECT run_id FROM _mr_runs WHERE status = 'error' LIMIT 1")$run_id[1]
      expect_warning(launch(mr_run(bad_id)), "status 'error'")
    })
  })
})

test_that("modelrunnR.relaunch_nonsuccess = 'silent' suppresses the warning", {
  withr::with_tempdir({
    withr::with_options(list(modelrunnR.relaunch_nonsuccess = "silent"), {
      .mr_with_test_db({
        tryCatch(launch({ stop("boom") }), error = function(e) NULL)
        con <- modelrunnR:::.mr_get_connection()
        bad_id <- DBI::dbGetQuery(con, "SELECT run_id FROM _mr_runs WHERE status = 'error' LIMIT 1")$run_id[1]
        expect_no_warning(launch(mr_run(bad_id)))
      })
    })
  })
})

test_that("modelrunnR.relaunch_nonsuccess = 'error' refuses with a clear message", {
  withr::with_tempdir({
    withr::with_options(list(modelrunnR.relaunch_nonsuccess = "error"), {
      .mr_with_test_db({
        tryCatch(launch({ stop("boom") }), error = function(e) NULL)
        con <- modelrunnR:::.mr_get_connection()
        bad_id <- DBI::dbGetQuery(con, "SELECT run_id FROM _mr_runs WHERE status = 'error' LIMIT 1")$run_id[1]
        expect_error(launch(mr_run(bad_id)), "modelrunnR.relaunch_nonsuccess")
      })
    })
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "launch-by-run-id")'
```
Expected: FAIL — no warning emitted.

- [ ] **Step 3: Implement the warn/error policy**

In `R/launch.R`, immediately after the `relaunch_mode` resolution block (after `if (is.na(label)) ...` lines updated in Task 2), add:

```r
    # Non-success source: warn / error / silent per option. Only
    # applies to mr_run() — mr_label() resolves by latest, which by
    # construction usually points at a success row.
    if (identical(relaunch_kind, "run") &&
        !is.na(resolved$status) &&
        !identical(resolved$status, "success")) {
      policy <- match.arg(
        getOption("modelrunnR.relaunch_nonsuccess", "warn"),
        c("warn", "error", "silent")
      )
      msg <- sprintf(
        "launch(): re-executing run_id '%s' whose source row has status '%s'.",
        code$value, resolved$status
      )
      if (identical(policy, "error")) {
        stop(paste(msg, "Set options(modelrunnR.relaunch_nonsuccess = \"warn\") to relaunch anyway."),
             call. = FALSE)
      }
      if (identical(policy, "warn")) warning(msg, call. = FALSE)
    }
```

- [ ] **Step 4: Run tests**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "launch-by-run-id")'
```
Expected: all Task 3 tests pass; Task 1 + 2 tests still pass.

- [ ] **Step 5: Commit**

```bash
git add R/launch.R tests/testthat/test-launch-by-run-id.R
git commit -m "feat(launch): warn (configurable) when relaunching by run id from non-success source"
```

---

## Task 4: Roxygen + NAMESPACE refresh for `launch()` and a NEWS bullet

**Files:**
- Modify: `R/launch.R` (roxygen block at top, ~line 23–37 and ~line 51–57)
- Modify: `NEWS.md`

- [ ] **Step 1: Update roxygen for `launch()`**

In the `@section Script, inline, and relaunch modes:` section near `R/launch.R:23-29`, replace the bullet:

```
#' - **Relaunch mode** -- `launch(mr_label("baseline"))` looks up the
#'   most recent run tagged with that label and re-executes its code.
```

with:

```
#' - **Relaunch mode** -- `launch(mr_label("baseline"))` looks up the
#'   most recent run tagged with that label and re-executes its code.
#'   `launch(mr_run(run_id))` re-executes the specific stored run by
#'   id; the source row's `variant_label` (if any) is auto-inherited
#'   onto the new run unless the caller passes an explicit `label`.
#'   Re-executing a run whose source status isn't `"success"` warns
#'   by default (configurable via
#'   `options(modelrunnR.relaunch_nonsuccess = c("warn","error","silent"))`).
```

In the `@param code` section at `R/launch.R:51-57`, replace:

```
#'   - [mr_label()] (relaunch mode -- re-executes the most recent run
#'     under that label).
```

with:

```
#'   - [mr_label()] (relaunch mode -- re-executes the most recent run
#'     under that label).
#'   - [mr_run()] (relaunch mode -- re-executes the specific stored
#'     run by id).
```

- [ ] **Step 2: Add NEWS entry**

Append to `NEWS.md` under the current development section (create `# modelrunnR (development version)` if not present):

```markdown
* `launch()` now accepts `mr_run(run_id)` as a first-argument reference,
  re-executing the specific stored run by id (in addition to the existing
  `mr_label()` form). Source row's `variant_label` is auto-inherited.
  Re-executing a non-`"success"` source row emits a `warning()` by
  default; configurable via
  `options(modelrunnR.relaunch_nonsuccess = c("warn","error","silent"))`.
```

- [ ] **Step 3: Regenerate man pages**

```
Rscript -e 'devtools::document()'
```

- [ ] **Step 4: Verify nothing else broke**

```
Rscript -e 'devtools::test()'
```
Expected: all green.

- [ ] **Step 5: Commit**

```bash
git add R/launch.R man/launch.Rd NEWS.md
git commit -m "docs(launch): document mr_run() relaunch mode + NEWS bullet"
```

---

## Task 5: Phase 1 verification — `R CMD check` clean

**Files:** none (verification only)

- [ ] **Step 1: Full check**

```
Rscript -e 'devtools::check()'
```
Expected: `0 errors, 0 warnings, 0 notes` (or only pre-existing notes — diff against the pre-Phase-1 baseline if unsure).

- [ ] **Step 2: If warnings/notes appeared, fix the underlying cause and re-run**

Do not suppress check output. Per framework invariant 2, warnings are surfaced, not swallowed.

- [ ] **Step 3: Phase 1 complete — checkpoint commit (no changes expected, but record the milestone in the next commit's message if doc regen reshuffled anything)**

If `devtools::document()` rewrote anything trivially, commit it now:

```bash
git status
git add -p   # be selective
git commit -m "chore(docs): regen man pages after launch(mr_run()) rollout"
```

---

# Phase 2 — `queue()`

Spec: `docs/superpowers/specs/2026-04-26-queue-design.md`. Twelve tasks. Phase 1 must be complete.

## Task 6: `queue()` skeleton + minimal inline write path

**Files:**
- Create: `R/queue.R`
- Test: `tests/testthat/test-queue.R`

- [ ] **Step 1: Write the failing test**

```r
# tests/testthat/test-queue.R

test_that("queue({ ... }) writes one row to _mr_runs with status='queued'", {
  withr::with_tempdir({
    .mr_with_test_db({
      r <- queue({ x <- 1 + 1 })
      expect_s3_class(r, "data.frame")
      expect_equal(nrow(r), 1L)
      expect_equal(r$status, "queued")
      expect_true(nzchar(r$run_id))
      expect_match(r$step, "^<inline:")
    })
  })
})

test_that("queue() returns invisibly", {
  withr::with_tempdir({
    .mr_with_test_db({
      result <- withVisible(queue({ x <- 1 }))
      expect_false(result$visible)
    })
  })
})

test_that("queued row's code_body matches the deparsed expression", {
  withr::with_tempdir({
    .mr_with_test_db({
      r <- queue({ z <- 42 })
      con <- modelrunnR:::.mr_get_connection()
      stored <- DBI::dbGetQuery(con, "SELECT code_body FROM _mr_runs WHERE run_id = ?",
                                params = list(r$run_id))$code_body[1]
      expect_match(stored, "z <- 42", fixed = TRUE)
    })
  })
})

test_that("queued row's execution columns are NA", {
  withr::with_tempdir({
    .mr_with_test_db({
      r <- queue({ x <- 1 })
      con <- modelrunnR:::.mr_get_connection()
      row <- DBI::dbGetQuery(con,
        "SELECT inputs, outputs, started_at, duration_ms, hostname, helpers, external_inputs
           FROM _mr_runs WHERE run_id = ?",
        params = list(r$run_id))
      # JSON columns: empty array sentinel, not NA, matches launch() conventions
      # for "nothing here". inputs/outputs/external_inputs/helpers all use "[]".
      expect_equal(row$inputs[1], "[]")
      expect_equal(row$outputs[1], "[]")
      expect_equal(row$external_inputs[1], "[]")
      expect_equal(row$helpers[1], "[]")
      # Timing + session-context: NA at queue time.
      expect_true(is.na(row$started_at[1]))
      expect_true(is.na(row$duration_ms[1]))
      expect_true(is.na(row$hostname[1]))
    })
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "queue")'
```
Expected: FAIL with `could not find function "queue"`.

- [ ] **Step 3: Write minimal `R/queue.R`**

```r
#' Stage a tracked run without executing it
#'
#' `queue()` registers a run row to `_mr_runs` with `status = "queued"`
#' but does not run the code. The queued row carries everything needed
#' to execute later: `step`, `code_body`, `code_hash`, `rebinds`,
#' `variant_label`, and `batch_id`. Pickup is the existing
#' [launch()] verb called with [mr_run()]:
#'
#' ```r
#' q <- queue({ fit_model(grab("x")) |> stow("model") })
#' launch(mr_run(q$run_id))   # picks up and executes; row updates in place
#' ```
#'
#' Parallel execution is not built in. Compose with `future`/`furrr`,
#' base R loops, or shell-level job runners (e.g. `tsp`, an HPC submit
#' script). modelrunnR records; the consumer executes.
#'
#' @param code A braced `{ ... }` block (inline) or a path to an
#'   `.R` script (file step). Reference objects ([mr_run()],
#'   [mr_label()], [mr_hash()]) and `.sql` paths / [mr_sql()] are
#'   rejected — re-queueing or staging SQL is out of scope.
#' @param rebind Optional named list, or [mr_binds()] / [mr_envelopes()]
#'   for batch staging (writes N queued rows under one `batch_id`).
#'   Same semantics as `launch(rebind = ...)`.
#' @param label Optional `variant_label` for the queued run.
#' @param duckdb_seed Optional numeric seed in `[-1, 1]`, recorded on
#'   the queued row and applied at pickup time.
#' @param ... Reserved for future arguments.
#'
#' @return The queued run record (one row of `_mr_runs`, or N rows for
#'   a batch) with `status = "queued"`, invisibly.
#' @export
queue <- function(code, rebind = NULL, label = NULL,
                  duckdb_seed = NULL, ...) {
  dots <- list(...)
  if (length(dots) > 0L) {
    stop(sprintf("queue(): unknown arguments: %s",
                 paste(names(dots), collapse = ", ")),
         call. = FALSE)
  }
  script_expr <- substitute(code)
  inline_mode <- is.call(script_expr) && identical(script_expr[[1]], as.name("{"))

  # Reject reference objects.
  if (!inline_mode && .mr_is_ref(code)) {
    stop(sprintf(
      "queue(): mr_%s() is not accepted as a first-argument reference. queue() stages new runs; re-queueing an existing stored run is incoherent.",
      code$kind
    ), call. = FALSE)
  }

  # Reject mr_sql() inline.
  if (!inline_mode && inherits(code, "mr_ref_sql")) {
    stop("queue(): SQL staging via mr_sql() is out of scope (v1).", call. = FALSE)
  }

  if (inline_mode) {
    code_body <- paste(deparse(script_expr, width.cutoff = 500L), collapse = "\n")
    expr_hash <- .mr_hash_bytes(charToRaw(code_body))
    step      <- sprintf("<inline:%s>", substr(expr_hash, 1L, 12L))
    code_hash <- .mr_code_hash_inline(code_body, list())
  } else {
    stopifnot(is.character(code), length(code) == 1L, nzchar(code))
    if (tolower(tools::file_ext(code)) == "sql") {
      stop("queue(): SQL file staging is out of scope (v1).", call. = FALSE)
    }
    if (!file.exists(code)) {
      stop(sprintf("queue(): file not found: %s", code), call. = FALSE)
    }
    step      <- normalizePath(code, mustWork = TRUE)
    code_body <- paste(readLines(step, warn = FALSE), collapse = "\n")
    code_hash <- .mr_code_hash(step, list())
  }

  label <- .mr_validate_label(label)

  if (!is.null(duckdb_seed)) {
    if (!is.numeric(duckdb_seed) || length(duckdb_seed) != 1L || is.na(duckdb_seed)) {
      stop("queue(): duckdb_seed must be a single numeric value.", call. = FALSE)
    }
    if (duckdb_seed < -1 || duckdb_seed > 1) {
      stop(sprintf("queue(): duckdb_seed must be in [-1, 1]; got %s.", duckdb_seed),
           call. = FALSE)
    }
  }

  .mr_get_connection()

  # Resolve rebinds at queue time so the JSON provenance reflects what
  # the user passed (matches launch()'s recording behavior).
  resolved_rebinds <- .mr_resolve_rebinds(rebind)

  # Batch path: split here.
  if (inherits(rebind, "mr_binds")) {
    return(invisible(.mr_queue_batch(
      step       = step,
      code_body  = code_body,
      code_hash  = code_hash,
      envelopes  = unclass(rebind),
      label      = label,
      duckdb_seed = duckdb_seed
    )))
  }

  run_id <- .mr_new_run_id()
  row <- .mr_write_run_row(
    step            = step,
    run_id          = run_id,
    inputs          = list(),
    outputs         = list(),
    started_at      = NA,
    duration_ms     = NA_integer_,
    status          = "queued",
    code_hash       = code_hash,
    external_inputs = list(files = list(), env = list()),
    helpers         = list(),
    variant_label   = label,
    code_body       = code_body,
    duckdb_seed     = if (is.null(duckdb_seed)) NA_real_ else duckdb_seed,
    rebinds         = resolved_rebinds$provenance,
    batch_id        = NA_character_,
    session_info    = .mr_blank_session_info()
  )
  invisible(row)
}

# Helper: blank session-context row (all NA), used by queue() so the
# session-context columns reflect "unknown — pickup will populate."
.mr_blank_session_info <- function() {
  list(
    hostname = NA_character_, os = NA_character_, arch = NA_character_,
    r_version = NA_character_, n_cpu = NA_integer_,
    total_ram_bytes = NA_real_, free_ram_bytes = NA_real_,
    attached_packages = NA_character_
  )
}

# Batch helper for queue(). Stub — extended in Task 8.
.mr_queue_batch <- function(step, code_body, code_hash, envelopes,
                            label, duckdb_seed) {
  stop("queue(): batch staging via mr_binds() is not yet implemented (Task 8).",
       call. = FALSE)
}
```

> **Note on `.mr_blank_session_info()`:** the existing `_mr_runs` schema may include a `git_sha` / `git_branch` / `git_dirty` set of columns (added by the recent `eddb3a8` commit). `.mr_write_run_row()` may need to accept (or default) those fields. Check the live signature of `.mr_write_run_row()` before this task — if it now requires git fields, extend the blank session helper similarly. Don't capture git context at queue time; it's a pickup-time concern.

- [ ] **Step 4: Run tests**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "queue")'
```
Expected: all Task 6 tests pass.

- [ ] **Step 5: Commit**

```bash
git add R/queue.R tests/testthat/test-queue.R
git commit -m "feat(queue): minimal inline staging — write status='queued' row, no execution"
```

---

## Task 7: File-step staging

**Files:**
- Modify: `tests/testthat/test-queue.R`
- (No code change expected — Task 6 already implemented the file branch. This task adds tests and verifies.)

- [ ] **Step 1: Write the failing test**

```r
# Append to tests/testthat/test-queue.R

test_that("queue('script.R') captures file bytes into code_body and computes code_hash", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines(c("x <- 'hello'", "stow(x, 'greeting')"), "fit.R")
      r <- queue("fit.R")
      expect_equal(r$status, "queued")
      expect_equal(normalizePath(r$step), normalizePath("fit.R"))
      con <- modelrunnR:::.mr_get_connection()
      stored <- DBI::dbGetQuery(con,
        "SELECT code_body, code_hash FROM _mr_runs WHERE run_id = ?",
        params = list(r$run_id))
      expect_match(stored$code_body[1], "hello", fixed = TRUE)
      expect_true(nzchar(stored$code_hash[1]))
    })
  })
})

test_that("queue('missing.R') errors clearly", {
  withr::with_tempdir({
    .mr_with_test_db({
      expect_error(queue("does_not_exist.R"), "file not found")
    })
  })
})

test_that("queue('foo.sql') is rejected (out of scope v1)", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines("SELECT 1", "f.sql")
      expect_error(queue("f.sql"), "out of scope")
    })
  })
})
```

- [ ] **Step 2: Run tests; expect all pass (Task 6 already covers the implementation)**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "queue")'
```

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-queue.R
git commit -m "test(queue): file-step staging + missing-file + .sql rejection"
```

---

## Task 8: Batch staging via `mr_binds()`

**Files:**
- Modify: `R/queue.R` (`.mr_queue_batch()`)
- Test: `tests/testthat/test-queue.R`

- [ ] **Step 1: Write the failing test**

```r
# Append to tests/testthat/test-queue.R

test_that("queue() with rebind = mr_binds() writes N queued rows under one batch_id", {
  withr::with_tempdir({
    .mr_with_test_db({
      r <- queue(
        { x <- grab("alpha") },
        rebind = mr_binds(alpha = c(0.1, 0.5, 1.0))
      )
      expect_equal(nrow(r), 3L)
      expect_true(all(r$status == "queued"))
      expect_equal(length(unique(r$batch_id)), 1L)
      expect_false(is.na(r$batch_id[1]))
      expect_equal(length(unique(r$run_id)), 3L)
    })
  })
})

test_that("each queued batch row's `rebinds` reflects its envelope's resolved rebinds", {
  withr::with_tempdir({
    .mr_with_test_db({
      r <- queue(
        { x <- grab("alpha") },
        rebind = mr_binds(alpha = c(0.1, 0.5))
      )
      con <- modelrunnR:::.mr_get_connection()
      rows <- DBI::dbGetQuery(con,
        "SELECT run_id, rebinds FROM _mr_runs WHERE batch_id = ? ORDER BY run_id",
        params = list(r$batch_id[1]))
      # Both rebinds JSONs should contain the literal values.
      expect_match(rows$rebinds[1], "0.1", fixed = TRUE)
      expect_match(rows$rebinds[2], "0.5", fixed = TRUE)
    })
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "queue")'
```
Expected: FAIL with `not yet implemented (Task 8)`.

- [ ] **Step 3: Replace `.mr_queue_batch()` stub in `R/queue.R`**

```r
.mr_queue_batch <- function(step, code_body, code_hash, envelopes,
                            label, duckdb_seed) {
  batch_id <- .mr_new_batch_id()
  rows <- vector("list", length(envelopes))
  for (i in seq_along(envelopes)) {
    env <- envelopes[[i]]
    env_label <- if (!is.null(env$.label)) env$.label else label
    env_label <- .mr_validate_label(env_label)
    env_rebind <- env[setdiff(names(env), ".label")]
    resolved <- .mr_resolve_rebinds(env_rebind)
    run_id <- .mr_new_run_id()
    rows[[i]] <- .mr_write_run_row(
      step            = step,
      run_id          = run_id,
      inputs          = list(),
      outputs         = list(),
      started_at      = NA,
      duration_ms     = NA_integer_,
      status          = "queued",
      code_hash       = code_hash,
      external_inputs = list(files = list(), env = list()),
      helpers         = list(),
      variant_label   = env_label,
      code_body       = code_body,
      duckdb_seed     = if (is.null(duckdb_seed)) NA_real_ else duckdb_seed,
      rebinds         = resolved$provenance,
      batch_id        = batch_id,
      session_info    = .mr_blank_session_info()
    )
  }
  do.call(rbind, rows)
}
```

> **Verify before writing:** `.mr_new_batch_id()` exists per the batch-launch spec — confirm the symbol via `grep -n '.mr_new_batch_id' R/`. If named differently in the live codebase (e.g. inlined into `.mr_launch_batch`), extract a small helper to one of `R/launch_batch.R` or `R/launch.R` and call it from both sites.

- [ ] **Step 4: Run tests; expect all pass**

- [ ] **Step 5: Commit**

```bash
git add R/queue.R tests/testthat/test-queue.R
git commit -m "feat(queue): batch staging via mr_binds() — N queued rows under one batch_id"
```

---

## Task 9: Reject incoherent first-arg refs

**Files:**
- Test: `tests/testthat/test-queue.R`
- (No code change expected — Task 6 already implemented the guard. This task adds tests.)

- [ ] **Step 1: Write the failing test**

```r
# Append to tests/testthat/test-queue.R

test_that("queue(mr_run(id)) errors", {
  withr::with_tempdir({
    .mr_with_test_db({
      r <- queue({ x <- 1 })
      expect_error(queue(mr_run(r$run_id)), "not accepted as a first-argument reference")
    })
  })
})

test_that("queue(mr_label('x')) errors", {
  withr::with_tempdir({
    .mr_with_test_db({
      expect_error(queue(mr_label("foo")), "not accepted as a first-argument reference")
    })
  })
})

test_that("queue(mr_hash('abc')) errors", {
  withr::with_tempdir({
    .mr_with_test_db({
      expect_error(queue(mr_hash("abc")), "not accepted as a first-argument reference")
    })
  })
})

test_that("queue(mr_sql('SELECT 1')) errors", {
  withr::with_tempdir({
    .mr_with_test_db({
      expect_error(queue(mr_sql("SELECT 1")), "out of scope")
    })
  })
})
```

- [ ] **Step 2: Run tests; expect all pass**

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-queue.R
git commit -m "test(queue): reject mr_run/mr_label/mr_hash/mr_sql as first arg"
```

---

## Task 10: Pickup — `launch(mr_run(id))` against a queued row updates in place

**Files:**
- Create: `R/launch_queued.R`
- Modify: `R/launch.R` (extend the resolver branch from Task 2 to detect `status = "queued"` and route to the in-place updater)
- Test: `tests/testthat/test-queue-pickup.R`

- [ ] **Step 1: Write the failing test**

```r
# tests/testthat/test-queue-pickup.R

test_that("launch(mr_run(id)) on a queued row updates the same row in place", {
  withr::with_tempdir({
    .mr_with_test_db({
      q <- queue({ x <- 21 + 21; stow(x, "answer") })
      r <- launch(mr_run(q$run_id))
      expect_equal(r$run_id, q$run_id)              # same id
      expect_equal(r$status, "success")             # status flipped
      expect_false(is.na(r$started_at))             # populated
      expect_false(is.na(r$duration_ms))
      con <- modelrunnR:::.mr_get_connection()
      n <- DBI::dbGetQuery(con,
        "SELECT COUNT(*) AS n FROM _mr_runs WHERE run_id = ?",
        params = list(q$run_id))$n
      expect_equal(n, 1L)                            # in-place: still one row
    })
  })
})

test_that("queued row's frozen columns are preserved across pickup", {
  withr::with_tempdir({
    .mr_with_test_db({
      q <- queue({ y <- 7 }, label = "exp_a")
      pre  <- modelrunnR:::.mr_get_connection() |>
        DBI::dbGetQuery("SELECT step, code_body, code_hash, variant_label, rebinds FROM _mr_runs WHERE run_id = ?",
                        params = list(q$run_id))
      launch(mr_run(q$run_id))
      post <- modelrunnR:::.mr_get_connection() |>
        DBI::dbGetQuery("SELECT step, code_body, code_hash, variant_label, rebinds FROM _mr_runs WHERE run_id = ?",
                        params = list(q$run_id))
      expect_equal(pre$step,          post$step)
      expect_equal(pre$code_body,     post$code_body)
      expect_equal(pre$code_hash,     post$code_hash)
      expect_equal(pre$variant_label, post$variant_label)
      expect_equal(pre$rebinds,       post$rebinds)
    })
  })
})

test_that("non-queued mr_run() relaunch still writes a new row (Phase 1 semantics unchanged)", {
  withr::with_tempdir({
    .mr_with_test_db({
      r1 <- launch({ x <- 1 })
      r2 <- launch(mr_run(r1$run_id))
      expect_false(r2$run_id == r1$run_id)         # new row, not in-place
    })
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "queue-pickup")'
```
Expected: FAIL — currently a queued-row pickup writes a new row.

- [ ] **Step 3: Create `R/launch_queued.R`**

```r
## In-place pickup of a queued run row.
##
## Called from launch() when launch(mr_run(id)) resolves a row whose
## status is "queued". Executes the row's code_body (or re-sources the
## file for file steps) and updates the row in place: status flips,
## execution columns populate, frozen columns (run_id, step, rebinds,
## variant_label, batch_id) stay.

.mr_pickup_queued_run <- function(run_id, resolved, label, external_inputs,
                                  force, duckdb_seed) {
  con <- .mr_get_connection()
  prior <- DBI::dbGetQuery(
    con,
    "SELECT step, code_body, code_hash, variant_label, rebinds, batch_id, duckdb_seed
       FROM _mr_runs WHERE run_id = ?",
    params = list(run_id)
  )
  if (nrow(prior) == 0L) {
    stop(sprintf(".mr_pickup_queued_run(): row vanished for run_id '%s'.", run_id),
         call. = FALSE)
  }

  step          <- resolved$step
  code_body     <- resolved$code_body
  relaunch_expr <- resolved$expr   # NULL for file steps that exist on disk
  inline_mode   <- startsWith(step, "<inline:")

  # Honor seed precedence: caller's overrides queued row's.
  if (is.null(duckdb_seed)) {
    queued_seed <- prior$duckdb_seed[1]
    duckdb_seed <- if (is.na(queued_seed)) NULL else queued_seed
  }

  # Honor label precedence: caller's overrides queued row's.
  if (is.na(label)) label <- prior$variant_label[1]

  # Reconstruct rebind from the queued row's resolved provenance JSON
  # so the executor sees the same name->value/ref bindings staged at
  # queue time.
  rebind <- .mr_rebinds_from_provenance_json(prior$rebinds[1])

  started_at   <- Sys.time()
  start_secs   <- as.numeric(started_at)
  session_info <- .mr_capture_session_info()

  resolved_ext     <- .mr_resolve_external_inputs(external_inputs)
  resolved_rebinds <- .mr_resolve_rebinds(rebind)
  rebinds_map      <- resolved_rebinds$map
  shape_b_filters  <- .mr_state$pending_shape_b_filters
  .mr_state$pending_shape_b_filters <- NULL

  # Freshness check at pickup, not queue time (per spec).
  staleness     <- .mr_is_stale(step, variant_label = label, rebind = rebinds_map)
  skip_on_fresh <- isTRUE(getOption("modelrunnR.skip_if_fresh", TRUE))
  will_skip     <- !staleness$stale && !isTRUE(force) && skip_on_fresh
  .mr_print_staleness(step, staleness, will_skip = will_skip)

  if (will_skip) {
    .mr_update_queued_row(
      run_id = run_id, status = "skipped_fresh",
      started_at = started_at, duration_ms = 0L,
      inputs = list(), outputs = list(),
      external_inputs = resolved_ext, helpers = list(),
      session_info = session_info,
      code_body = code_body,
      code_hash = if (inline_mode) prior$code_hash[1] else .mr_code_hash(step, list()),
      variant_label = label
    )
    return(invisible(.mr_runs_row(run_id)))
  }

  if (!is.null(duckdb_seed)) {
    DBI::dbExecute(.mr_get_connection(), "SELECT setseed(?)",
                   params = list(duckdb_seed))
  }

  .mr_guard_no_nested_launch()
  .mr_start_recording(run_id = run_id, variant_label = label)
  .mr_start_helper_tracking()
  .mr_start_rebinding(rebinds_map, shape_b_filters)
  on.exit({
    if (.mr_is_recording()) .mr_stop_recording()
    if (!is.null(.mr_state$helpers)) .mr_stop_helper_tracking()
    .mr_stop_rebinding()
  }, add = TRUE)

  status  <- "success"
  err_obj <- NULL
  tryCatch(
    if (inline_mode) {
      .mr_eval_inline(parse(text = code_body))
    } else if (!is.null(relaunch_expr)) {
      .mr_eval_inline(relaunch_expr)
    } else {
      .mr_source_script(step)
    },
    error = function(e) { status <<- "error"; err_obj <<- e }
  )

  rec     <- .mr_stop_recording()
  helpers <- .mr_stop_helper_tracking()
  duration_ms <- as.integer(round((as.numeric(Sys.time()) - start_secs) * 1000))

  code_hash <- if (inline_mode) {
    .mr_code_hash_inline(code_body, helpers)
  } else {
    .mr_code_hash(step, helpers)
  }

  .mr_update_queued_row(
    run_id = run_id, status = status,
    started_at = started_at, duration_ms = duration_ms,
    inputs = rec$inputs, outputs = rec$outputs,
    external_inputs = resolved_ext, helpers = helpers,
    session_info = session_info,
    code_body = code_body, code_hash = code_hash,
    variant_label = label
  )

  if (!is.null(err_obj)) stop(err_obj)
  invisible(.mr_runs_row(run_id))
}

# UPDATE _mr_runs SET ... WHERE run_id = ?
.mr_update_queued_row <- function(run_id, status, started_at, duration_ms,
                                  inputs, outputs, external_inputs, helpers,
                                  session_info, code_body, code_hash,
                                  variant_label) {
  con <- .mr_get_connection()
  DBI::dbExecute(con,
    "UPDATE _mr_runs SET
        status            = ?,
        started_at        = ?,
        duration_ms       = ?,
        inputs            = ?,
        outputs           = ?,
        external_inputs   = ?,
        helpers           = ?,
        hostname          = ?,
        os                = ?,
        arch              = ?,
        r_version         = ?,
        n_cpu             = ?,
        total_ram_bytes   = ?,
        free_ram_bytes    = ?,
        attached_packages = ?,
        code_body         = ?,
        code_hash         = ?,
        variant_label     = ?
      WHERE run_id = ?",
    params = list(
      status, started_at, duration_ms,
      .mr_pairs_to_json(inputs), .mr_pairs_to_json(outputs),
      .mr_external_inputs_to_json(external_inputs),
      .mr_helpers_to_json(helpers),
      session_info$hostname, session_info$os, session_info$arch,
      session_info$r_version, session_info$n_cpu,
      session_info$total_ram_bytes, session_info$free_ram_bytes,
      session_info$attached_packages,
      code_body, code_hash, variant_label,
      run_id
    )
  )
}

# Read back a single _mr_runs row for return value parity with launch().
.mr_runs_row <- function(run_id) {
  con <- .mr_get_connection()
  DBI::dbGetQuery(con, "SELECT * FROM _mr_runs WHERE run_id = ?", params = list(run_id))
}

# Reconstruct a rebind list from the queued row's provenance JSON.
# Provenance entries shape (per resolve_rebinds): {name, kind, value, ...}.
# For literals, return value as-is. For refs, reconstruct the matching
# mr_*() ref. Implementation should mirror .mr_resolve_rebinds() in
# reverse: see R/rebind.R / R/references.R for the entry shapes.
.mr_rebinds_from_provenance_json <- function(json_text) {
  if (is.na(json_text) || json_text == "[]" || !nzchar(json_text)) return(NULL)
  entries <- jsonlite::fromJSON(json_text, simplifyVector = FALSE)
  out <- list()
  for (e in entries) {
    if (identical(e$kind, "literal")) {
      out[[e$name]] <- e$value
    } else if (identical(e$kind, "variant")) {
      out[[e$name]] <- mr_variant(e$value)
    } else if (identical(e$kind, "hash")) {
      out[[e$name]] <- mr_hash(e$value)
    } else if (identical(e$kind, "run")) {
      out[[e$name]] <- mr_run(e$value)
    } else if (identical(e$kind, "as_of")) {
      out[[e$name]] <- mr_as_of(e$value)
    }
  }
  out
}
```

> **Note on `.mr_rebinds_from_provenance_json()`:** the JSON entry shape is dictated by `.mr_resolve_rebinds()`'s provenance writer. Verify the exact keys (`kind`, `value`, possibly nested) by reading `R/rebind.R` and adjust the parser to match. The Task 10 tests don't exercise rebinds; Task 11 will. If you discover the round-trip needs more fields preserved, surface this as an ASK before extending blindly.

- [ ] **Step 4: Modify `R/launch.R` to route queued pickups**

After the `relaunch_mode` resolution block (after the Task 3 warn/error policy), and *before* the `.mr_launch_one()` call at the bottom of `launch()`, add:

```r
  if (identical(relaunch_kind, "run") &&
      identical(resolved$status, "queued")) {
    return(.mr_pickup_queued_run(
      run_id          = code$value,
      resolved        = resolved,
      label           = label,
      external_inputs = external_inputs,
      force           = force,
      duckdb_seed     = duckdb_seed
    ))
  }
```

The "warn on non-success" policy from Task 3 should NOT fire for `"queued"` rows — they are an expected pre-execution state, not a failed prior run. Update the Task 3 condition:

Replace:
```r
    if (identical(relaunch_kind, "run") &&
        !is.na(resolved$status) &&
        !identical(resolved$status, "success")) {
```

with:
```r
    if (identical(relaunch_kind, "run") &&
        !is.na(resolved$status) &&
        !identical(resolved$status, "success") &&
        !identical(resolved$status, "queued")) {
```

- [ ] **Step 5: Run tests**

```
Rscript -e 'devtools::load_all(); devtools::test(filter = "queue-pickup")'
```
Expected: all Task 10 tests pass.

- [ ] **Step 6: Run full suite for regressions**

```
Rscript -e 'devtools::load_all(); devtools::test()'
```
Expected: all green.

- [ ] **Step 7: Commit**

```bash
git add R/launch.R R/launch_queued.R tests/testthat/test-queue-pickup.R
git commit -m "feat(launch): pickup queued runs in place via launch(mr_run(id))"
```

---

## Task 11: Pickup — freshness check sets `skipped_fresh` in place

**Files:**
- Test: `tests/testthat/test-queue-pickup.R`

- [ ] **Step 1: Write the failing test**

```r
# Append to tests/testthat/test-queue-pickup.R

test_that("queued-row pickup that would be skipped_fresh sets the row's status to skipped_fresh in place", {
  withr::with_tempdir({
    .mr_with_test_db({
      # First, run the same expression to success so a fresh prior exists.
      r1 <- launch({ x <- 1 + 1; stow(x, "two") })
      # Now queue an identical body.
      q  <- queue({ x <- 1 + 1; stow(x, "two") })
      # Pickup: should see the freshness check, skip, and stamp the
      # queued row as skipped_fresh — not execute, not error.
      r2 <- launch(mr_run(q$run_id))
      expect_equal(r2$run_id, q$run_id)
      expect_equal(r2$status, "skipped_fresh")
    })
  })
})
```

- [ ] **Step 2: Run test; expect it to pass (Task 10 already implemented the skip path)**

If it fails, the freshness branch in `.mr_pickup_queued_run()` is not flipping status. Inspect — the most likely cause is `.mr_update_queued_row()` not being called from the `will_skip` branch.

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-queue-pickup.R
git commit -m "test(queue): pickup honors freshness — sets skipped_fresh in place"
```

---

## Task 12: Pickup — file-step drift warning + `code_*` refresh

**Files:**
- Modify: `R/launch_queued.R` — emit drift warning, ensure `code_body`/`code_hash` are refreshed from the re-sourced file.
- Test: `tests/testthat/test-queue-pickup.R`

- [ ] **Step 1: Write the failing test**

```r
# Append to tests/testthat/test-queue-pickup.R

test_that("file-step queued pickup re-sources from disk and warns when content drifted", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines("x <- 'before'; stow(x, 'g')", "fit.R")
      q <- queue("fit.R")
      writeLines("x <- 'after';  stow(x, 'g')", "fit.R")  # edit
      expect_warning(launch(mr_run(q$run_id)), "drifted")
      # Refreshed columns reflect the executed (new) bytes.
      con <- modelrunnR:::.mr_get_connection()
      post <- DBI::dbGetQuery(con,
        "SELECT code_body, code_hash, status FROM _mr_runs WHERE run_id = ?",
        params = list(q$run_id))
      expect_match(post$code_body, "after", fixed = TRUE)
      expect_equal(post$status, "success")
    })
  })
})

test_that("file-step queued pickup with no drift does not warn", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines("x <- 'stable'; stow(x, 'g')", "fit.R")
      q <- queue("fit.R")
      expect_no_warning(launch(mr_run(q$run_id)))
    })
  })
})

test_that("file-step queued pickup falls back to the snapshot when the file is gone", {
  withr::with_tempdir({
    .mr_with_test_db({
      writeLines("x <- 'snap'; stow(x, 'g')", "fit.R")
      q <- queue("fit.R")
      file.remove("fit.R")
      expect_message(launch(mr_run(q$run_id)), "is gone from disk")
      con <- modelrunnR:::.mr_get_connection()
      post <- DBI::dbGetQuery(con, "SELECT status FROM _mr_runs WHERE run_id = ?",
                              params = list(q$run_id))
      expect_equal(post$status, "success")
    })
  })
})
```

- [ ] **Step 2: Run tests to verify the drift-warn one fails**

The "no drift" and "gone file" tests likely pass via the existing resolver. The drift-warn test will fail until we add the warning.

- [ ] **Step 3: Add drift detection to `.mr_pickup_queued_run()`**

In `R/launch_queued.R`, after the resolver returns and before execution, add:

```r
  # File-step drift detection. The resolver's `code_body` came from
  # disk (or the snapshot fallback); compare its hash against the
  # queued row's `code_hash` to detect "the file you queued isn't the
  # file you're about to run." Inline rows can't drift — `code_body`
  # came from the queued row itself.
  if (!inline_mode && file.exists(step)) {
    fresh_hash <- .mr_code_hash(step, list())
    queued_hash <- prior$code_hash[1]
    if (!is.na(queued_hash) && !identical(fresh_hash, queued_hash)) {
      warning(sprintf(
        "launch(mr_run('%s')): the file '%s' has drifted since queue time (queued hash %s, current %s). Re-sourcing from disk.",
        run_id, step,
        substr(queued_hash, 1L, 12L), substr(fresh_hash, 1L, 12L)
      ), call. = FALSE)
    }
  }
```

`code_body` and `code_hash` are already refreshed by the existing `.mr_update_queued_row()` call at the end of the function (it passes the post-execution `code_hash` and the resolver's `code_body`). No change to the update call needed.

- [ ] **Step 4: Run tests; expect all pass**

- [ ] **Step 5: Commit**

```bash
git add R/launch_queued.R tests/testthat/test-queue-pickup.R
git commit -m "feat(queue): warn on file-step drift between queue and pickup; refresh code_* in place"
```

---

## Task 13: `is_stale()` excludes queued rows

**Files:**
- Modify: `R/is_stale.R` (add `AND status != 'queued'` to "last run" lookups)
- Test: `tests/testthat/test-is-stale.R` (extend) or new `tests/testthat/test-queue-is-stale.R`

- [ ] **Step 1: Write the failing test**

```r
# tests/testthat/test-queue-is-stale.R

test_that("is_stale() ignores queued rows when computing freshness", {
  withr::with_tempdir({
    .mr_with_test_db({
      # First a successful run, then a queued same-body row.
      r1 <- launch({ x <- 1 + 1 })
      q  <- queue({ x <- 1 + 1 })
      # The queued row should NOT shadow r1 in the staleness lookup.
      # is_stale() against the inline step should still say fresh.
      expect_false(is_stale(r1$step)$stale)
    })
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

If the test passes already, `is_stale()` happens to be safe — verify by reading `R/is_stale.R` and confirming the queries don't match `status = 'queued'` rows. If they do, the test should be expanded to a stricter scenario (e.g., a queued row with mismatched code_hash that would falsely report "stale").

- [ ] **Step 3: Modify the queries in `R/is_stale.R`**

Locate the `_mr_runs` lookups in `R/is_stale.R` (likely an `ORDER BY started_at DESC LIMIT 1` shape). Add `AND status != 'queued'` to each WHERE clause.

> **Verify before editing:** read `R/is_stale.R` (57 lines) and identify exactly how many queries need the predicate. Apply consistently.

- [ ] **Step 4: Run test; expect it to pass**

- [ ] **Step 5: Run full suite**

```
Rscript -e 'devtools::test()'
```
Expected: all green.

- [ ] **Step 6: Commit**

```bash
git add R/is_stale.R tests/testthat/test-queue-is-stale.R
git commit -m "fix(is_stale): exclude queued rows from freshness lookups"
```

---

## Task 14: Status-enum audit — handle `"queued"` wherever `status` is branched on

**Files:** Discovery via grep, then targeted edits as needed.

- [ ] **Step 1: Locate every site that branches on `status`**

```bash
grep -rn 'status *== *"' R/
grep -rn 'switch(status' R/
grep -rn 'status %in% c(' R/
```

- [ ] **Step 2: For each site, decide whether `"queued"` needs explicit handling**

Document the audit inline as a Markdown checklist. For each site:

```
- [ ] R/foo.R:LINE  — context: <what this branch does>; decision: queued <handles correctly | needs to be added to whitelist | needs a new branch>
```

- [ ] **Step 3: Apply edits**

For sites that need `"queued"` added (e.g. `runs()` summary printing, propagation logic, prune filters), make the change. For sites where the existing logic already does the right thing for unknown statuses, no change.

- [ ] **Step 4: Add a regression test for any site touched**

If you change a branch, write a test that exercises queued status against that codepath. Don't trust the audit silently.

- [ ] **Step 5: Run full suite**

- [ ] **Step 6: Commit**

```bash
git add R/ tests/testthat/
git commit -m "chore(queue): audit status branches and handle 'queued' where needed"
```

---

## Task 15: Vignette section + NEWS bullet

**Files:**
- Modify: `vignettes/getting-started.Rmd`
- Modify: `NEWS.md`

- [ ] **Step 1: Add a "Staging runs for later" section to the vignette**

Add after the "Re-running a pipeline by label" section in `vignettes/getting-started.Rmd`:

````markdown
## Staging runs without executing them

`queue()` registers a run to `_mr_runs` with `status = "queued"` but
doesn't execute the code. Pickup is `launch(mr_run(id))` — the same
verb you'd use to relaunch a stored run, now also executing queued
rows in place.

```{r queue-stage, eval = FALSE}
# Stage a single run.
q <- queue({
  fit <- glm(y ~ x, data = grab("training"))
  stow(fit, "model")
})

# Pick it up later — anywhere, any process.
launch(mr_run(q$run_id))
```

Batch staging via `mr_binds()` writes N queued rows under one
`batch_id`:

```{r queue-batch, eval = FALSE}
qs <- queue(
  { fit_with(grab("alpha")) |> stow("model") },
  rebind = mr_binds(alpha = c(0.1, 0.5, 1.0))
)

# Serial pickup:
purrr::walk(qs$run_id, ~ launch(mr_run(.x)))

# Or parallel via future / furrr — modelrunnR doesn't drive
# parallelism itself; compose it from the consumer side.
# furrr::future_map(qs$run_id, ~ launch(mr_run(.x)))
```

Pickup is in-place: `run_id`, `step`, `code_body`, and `rebinds` stay
frozen across queue → pickup, while `status`, timing, and
session-context columns populate at execution. If the freshness check
would skip the run, the queued row's `status` becomes `"skipped_fresh"`
without executing the body.
````

- [ ] **Step 2: Add NEWS entry**

Append to `NEWS.md` under the current development section:

```markdown
* New `queue()` verb: register a run to `_mr_runs` with
  `status = "queued"` without executing it. Pickup is
  `launch(mr_run(id))`, which now also drains queued rows in place
  (preserves `run_id`, `step`, `code_body`, `rebinds`; populates
  `status`, timing, session-context). Batch staging via
  `rebind = mr_binds(...)` writes N queued rows under one `batch_id`.
  Parallelism is composed by the caller (`future`/`furrr`/shell);
  modelrunnR records and resumes, no built-in worker.
* New status value `"queued"` joins the existing set
  (`success`, `error`, `skipped_fresh`, `interactive`). No schema
  migration — `_mr_runs` columns are unchanged.
```

- [ ] **Step 3: Knit the vignette to verify it builds**

```
Rscript -e 'rmarkdown::render("vignettes/getting-started.Rmd")'
```

Expected: clean render, no errors. Delete the rendered `.html` afterward (it's gitignored anyway, but be tidy).

- [ ] **Step 4: Commit**

```bash
git add vignettes/getting-started.Rmd NEWS.md
git commit -m "docs(queue): vignette section + NEWS bullets"
```

---

## Task 16: `R CMD check` clean

**Files:** none (verification only)

- [ ] **Step 1: Regenerate man pages**

```
Rscript -e 'devtools::document()'
git status
```

Commit any non-trivial regen separately:

```bash
git add man/
git commit -m "chore(docs): regen man pages for queue() and launch(mr_run())"
```

- [ ] **Step 2: Full check**

```
Rscript -e 'devtools::check()'
```

Expected: `0 errors, 0 warnings, 0 notes` (or only pre-existing notes). Per framework invariant 2, warnings are surfaced — investigate root cause, do not suppress.

- [ ] **Step 3: Verify the practicum repo isn't broken**

Per framework invariant 1, `../practicum_repos/final_practicum` must keep working. Smoke-test by running its existing scripts that call `launch()`:

```bash
ls ../../../practicum_repos/final_practicum/qmd/   # confirm scripts exist
# If a top-level pipeline / quarto file uses launch(), render it once
# under load_all() to confirm no regressions:
Rscript -e 'devtools::load_all("/workspace/r-packages/modelrunnR"); rmarkdown::render("/workspace/practicum_repos/final_practicum/qmd/<...>.qmd")'
```

If anything breaks: SURFACE before committing further. Per the framework, the practicum is an invariant.

- [ ] **Step 4: Final commit if regen happened**

```bash
git status   # should be clean
```

If clean, the implementation is complete. Otherwise stage and commit any leftover regen.

---

## Summary of Tasks

| # | Phase | Description |
|---|-------|-------------|
| 1 | 1 | Resolver `.mr_resolve_relaunch_run_id()` |
| 2 | 1 | `launch()` accepts `mr_run()` first arg |
| 3 | 1 | Warn on relaunching non-success row + option |
| 4 | 1 | Roxygen + NEWS for `launch(mr_run())` |
| 5 | 1 | Phase 1 `R CMD check` clean |
| 6 | 2 | `queue()` skeleton + minimal inline write |
| 7 | 2 | File-step staging tests |
| 8 | 2 | Batch staging via `mr_binds()` |
| 9 | 2 | Reject incoherent first-arg refs |
| 10 | 2 | Pickup updates row in place |
| 11 | 2 | Pickup honors freshness → `skipped_fresh` in place |
| 12 | 2 | File-step drift warning + `code_*` refresh |
| 13 | 2 | `is_stale()` excludes queued rows |
| 14 | 2 | Status-enum audit |
| 15 | 2 | Vignette + NEWS |
| 16 | 2 | Final `R CMD check` + practicum smoke test |

Total: 16 tasks across two phases. Estimated 4–8 hours for an executor familiar with the codebase, longer if exploration is needed.
