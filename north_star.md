# The Code Database Accumulator

This package, which is morphing from and absorbing current version of `modelrunnR`, is for accumulating and using a database of code. Name? Pending. We'll call it the code accumulator.

grab and stow replace specific inputs and outputs.

launch, queue, third verb (at least).

I feel like as I write code, it should get easier to reuse. 

I think inside the code db, those scripts become functions.