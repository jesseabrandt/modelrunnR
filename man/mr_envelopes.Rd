% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_binds.R
\name{mr_envelopes}
\alias{mr_envelopes}
\title{Build batch envelopes by hand}
\usage{
mr_envelopes(...)
}
\arguments{
\item{...}{One or more named lists, each describing a single
envelope.}
}
\value{
An \code{mr_binds} object.
}
\description{
Primitive constructor under \code{mr_binds()}. Use this when you want
per-envelope \code{.label}, mixed reference kinds across envelopes, or
any other shape that the simpler \code{mr_binds()} sweep API doesn't
express.
}
\details{
Each \code{...} argument is a named list. A \code{.label} field, if present,
is the explicit run label for that envelope; all other names are
rebind slots (their values flow through resolution unchanged, exactly
like values inside \code{launch(rebind = list(...))}).
}
\seealso{
\code{\link[=mr_binds]{mr_binds()}} is the sugared form; reach for \code{mr_envelopes()}
only when you need per-envelope \code{.label} or mixed reference kinds
across envelopes.
}
