% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/references.R
\name{references}
\alias{references}
\alias{mr_hash}
\alias{mr_run}
\alias{mr_variant}
\alias{mr_label}
\alias{mr_as_of}
\title{Reference constructors for addressing stored modelrunnR entities}
\usage{
mr_hash(hash)

mr_run(run_id)

mr_variant(label)

mr_label(label)

mr_as_of(time)
}
\arguments{
\item{hash}{A content hash string (from \code{versions()}).}

\item{run_id}{A run id string (from \verb{_mr_runs}).}

\item{label}{A variant label string.}

\item{time}{A timestamp (\code{POSIXct}) or a string parseable by
\code{as.POSIXct}.}
}
\value{
A tagged list for \code{launch()} to resolve.
}
\description{
Small, structured wrappers for addressing existing modelrunnR
versions or runs by identity. Each returns a tagged list that
\code{launch()} interprets depending on where it appears:
}
\details{
\itemize{
\item Inside \verb{launch(rebind = list(<name> = <ref>))}, a reference
resolves to a content hash that overrides the named \code{grab()}
inside the script.
\item As \code{launch()}'s first argument:
\itemize{
\item \code{mr_label()} addresses a labeled pipeline's most recent run
and re-executes its code.
\item \code{mr_run()} addresses a stored run by id. Against a row whose
status is \code{"queued"} (see \code{\link[=queue]{queue()}}), the call updates that row
in place; against any other status it re-executes the stored
code as a new row.
}
}

Use a bare R value in \code{rebind} when you want the value stowed
inline; use one of these constructors when you want to address
something already stored.
}
