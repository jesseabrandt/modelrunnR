% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prune.R
\name{prune_runs}
\alias{prune_runs}
\title{Prune rows from run-indexed append logs (Shape B)}
\usage{
prune_runs(
  name = NULL,
  run_id = NULL,
  older_than = NULL,
  keep = NULL,
  force = FALSE
)
}
\arguments{
\item{name}{Optional logical name to restrict pruning to.}

\item{run_id}{Optional run id (single string or character vector) —
prune rows produced by those runs.}

\item{older_than}{Optional duration string (e.g. \code{"30d"}, \code{"6h"})
— prune rows whose run started before now - \if{html}{\out{<duration>}}.}

\item{keep}{Optional integer. Keep the N most recent runs per
logical name; prune rows from older runs.}

\item{force}{Logical. If \code{TRUE}, also prune variant-labeled rows.}
}
\value{
A data frame of per-name pruning summaries, invisibly.
}
\description{
Removes rows from append tables by run id or age. The registry row
in \verb{_mr_append_tables} is preserved even when all rows are removed,
so the accumulator continues to exist (empty) under its logical name.
}
\details{
Rows whose run has a non-null \code{variant_label} are protected unless
\code{force = TRUE}.
}
