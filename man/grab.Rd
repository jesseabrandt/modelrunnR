% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grab.R
\name{grab}
\alias{grab}
\title{Retrieve a value from the modelrunnR artifact store}
\usage{
grab(
  name,
  version = NULL,
  from_run = NULL,
  as_of = NULL,
  source = NULL,
  variant = NULL,
  run = NULL
)
}
\arguments{
\item{name}{A length-one character vector naming a logical value.}

\item{version}{Optional content hash (as returned by \code{\link[=versions]{versions()}})
to select a specific stored version. Shape A only.}

\item{from_run}{Optional run id (as returned by \code{\link[=launch]{launch()}}'s
invisibly-returned run row) to select the exact version produced
by that run. For Shape B tables, filters to rows from that run.}

\item{as_of}{Optional \code{POSIXct} timestamp; returns the version
that was latest at that time. Shape A only.}

\item{source}{Optional path to a CSV or Parquet file. Triggers an
implicit \code{stow(mr_file(source), name)} when the file hash
differs from (or is not yet present in) the stored source
metadata.}

\item{variant}{Optional string; resolves to the latest version produced
by any run launched with \code{label = variant}. Mutually exclusive with
\code{version}, \code{from_run}, \code{as_of}, and \code{run}. See \emph{Variants and
swappability} in docs/design.md for the full semantics.}

\item{run}{Cross-history selector. On Shape B (append tables), a run id
string filters to that run's rows, or \code{"all"} returns every row with
\code{run_id} and \code{variant_label} exposed. On Shape A (versioned), only
\code{"all"} is accepted and returns a named list of every stored version
keyed by \code{content_hash}.}
}
\value{
For tabular stored values (ingested files, stowed data
frames), a \code{dbplyr} lazy \code{tbl} bound to the modelrunnR DuckDB
connection. Compose \code{dplyr} verbs against it and call
\code{\link[dplyr:compute]{dplyr::collect()}} (or \code{\link[=as.data.frame]{as.data.frame()}} / \code{\link[tibble:as_tibble]{tibble::as_tibble()}})
to materialize. For non-tabular artifacts, the deserialized R
object.
}
\description{
modelrunnR stores tabular values two ways — as \strong{versioned} snapshots
(one row per distinct content) and as \strong{append tables} (one growing
table, row-stamped per run). \code{grab()} dispatches on which shape \code{name}
was stored as, so the same call works regardless. The distinction is
explained in the Getting Started vignette.
}
\details{
\strong{Versioned (Shape A)} — data the package treats as immutable: ingested
reference data, non-tabular artifacts (models, lists, results). The
default returns the current latest version; historical versions can be
selected via \code{version} (content hash), \code{from_run} (run id), or \code{as_of}
(timestamp), in that precedence order. Pass \code{run = "all"} to get a
\strong{named list} of every stored version (one element per content hash,
ordered oldest -> newest).

\strong{Append (Shape B)} — data frames you \code{stow()} inside runs. The default
returns one coherent snapshot — the rows from a single run — with system
columns (\verb{_mr_run_id}, \verb{_mr_variant_label}) stripped, so \code{grab(name)}
gives you user columns only. Which run depends on context: inside a
\code{\link[=launch]{launch()}} block it is the \emph{current} run (rows this run has written so
far); outside a launch it is the \emph{latest} run that wrote to \code{name}. The
exploratory workflow — \code{grab("metrics") |> collect()} at the REPL —
thus pulls a clean slice rather than the accumulated cross-run pile.
Pass \code{run = "all"} to opt into the full-history view: every row, with
system columns surfaced as user-facing \code{run_id} and \code{variant_label}
columns — the right lens for comparing runs.

Inside a tracked \code{\link[=launch]{launch()}}, the read is recorded as an input
\verb{\{name, hash\}} pair on the run row. Outside a launch, the read
is not logged.

When \code{source} is supplied, \code{grab()} behaves as an idempotent
read-or-ingest: if \code{name} does not exist yet, an implicit
\code{stow(mr_file(source), name)} is performed. If \code{name} exists and
the file's current content hash differs from the latest stored
\code{source_hash}, the file is re-stowed and a new version is created.
If the file is unchanged, the cached version is returned.
}
\section{Security note}{

Artifacts stored via \code{\link[=stow]{stow()}} are deserialized on read with
\code{qs2::qs_deserialize()}. Opening a project produced by someone else
trusts the artifacts to the same extent as trusting that party's R
code: \code{qs2} does not have \code{readRDS}'s historical callback
arbitrary-code-execution surface, but it has not been independently
audited. Do not \code{grab()} from projects you would not \code{source()}.
}

