% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_sql.R
\name{mr_sql}
\alias{mr_sql}
\title{Mark an inline SQL string for \code{launch()}}
\usage{
mr_sql(body)
}
\arguments{
\item{body}{A length-one character string containing the SQL body
plus any header lines.}
}
\value{
A tagged list with class \code{c("mr_ref_sql", "mr_ref")} for
\code{launch()} to dispatch on.
}
\description{
Wraps a SQL \code{SELECT} (or \verb{WITH ... SELECT}) string so \code{\link[=launch]{launch()}}
treats it as a SQL step. Inline equivalent of passing a \code{.sql} file
path to \code{launch()}.
}
\details{
The body must be a single bare query (no \code{CREATE}, no \code{INSERT},
exactly one statement). Optional declarative headers go above the
body as \verb{--} comment lines:
\itemize{
\item \verb{-- @inputs: <name>[, <name>...]} — required if the body
references any modelrunnR-managed name.
\item \verb{-- @output: <name>} — required for inline mode (no filename to
derive an output name from).
}

modelrunnR owns the \verb{CREATE OR REPLACE VIEW} (or \code{TABLE}) wrapper
around the body; users supply only the query.
}
\examples{
\dontrun{
# Assume `panel_raw` has already been stowed or ingested.
launch(mr_sql("
  -- @inputs: panel_raw
  -- @output: features
  SELECT firm_id, year,
         lag(sales) OVER (PARTITION BY firm_id ORDER BY year) AS lag_sales
  FROM panel_raw
"))

grab("features") |> dplyr::collect()
}
}
