% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/is_stale.R
\name{is_stale}
\alias{is_stale}
\title{Check whether a labeled pipeline's most recent run is stale}
\usage{
is_stale(ref)
}
\arguments{
\item{ref}{A \code{mr_label()} or \code{mr_variant()} reference. Other
reference constructors (\code{mr_hash}, \code{mr_run}, \code{mr_as_of}) address
stored content at a point in time; they don't map to the
"pipeline identity" staleness is about and error here.}
}
\value{
A logical scalar (\code{TRUE} if stale, \code{FALSE} if fresh) with
a \code{reasons} attribute carrying the same reason codes that
\code{launch()}'s advisory message prints (e.g., \code{"never_run"},
\code{"code"}, \code{"input:<name>"}, \code{"external:<path>"},
\code{"external:env:<NAME>"}).
}
\description{
Exposes modelrunnR's internal staleness check so users can gate their
own logic on whether a re-run would be a no-op. \code{is_stale()} takes a
reference to a \emph{labeled} pipeline, so first give a \code{launch()} a \code{label},
then ask about that label with \code{mr_label()}:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{# 1. Run a pipeline under a label.
launch(\{
  embeddings <- embed(grab("docs"))
  stow(embeddings, "embeddings")
\}, label = "embed")

# 2. Ask whether that labeled pipeline is now stale.
is_stale(mr_label("embed"))
#> [1] FALSE
#> attr(,"reasons")
#> character(0)

# 3. Typical use: re-run only when something changed.
if (is_stale(mr_label("embed"))) \{
  launch(\{
    embeddings <- embed(grab("docs"))
    stow(embeddings, "embeddings")
  \}, label = "embed")
\}
}\if{html}{\out{</div>}}

For most workflows this is unnecessary — \code{launch()} skips fresh runs
automatically (see the \code{force} argument and the
\code{modelrunnR.skip_if_fresh} option). \code{is_stale()} is the explicit
escape hatch for the case where the user wants to branch on
staleness without entering \code{launch()} at all.
}
