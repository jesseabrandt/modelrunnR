% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_file.R
\name{mr_file}
\alias{mr_file}
\title{Tag a file path as a modelrunnR file source}
\usage{
mr_file(path)
}
\arguments{
\item{path}{Length-1 non-empty character: a path to a \code{.csv},
\code{.tsv}, or \code{.parquet} file.}
}
\value{
A length-1 character vector of class
\code{c("mr_file", "character")}.
}
\description{
Wraps a path in a class (\code{mr_file}) that \code{\link[=stow]{stow()}} dispatches on, so
a file source flows through the same \code{stow()} verb as any other
value. Validation is lazy — the file does not need to exist at
construction time; existence is checked when the value is handed to
\code{stow()}.
}
\examples{
\dontrun{
stow(mr_file("data/training.parquet"), "training")
}
}
