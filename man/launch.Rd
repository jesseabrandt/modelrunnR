% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch.R
\name{launch}
\alias{launch}
\title{Launch a tracked modelrunnR step}
\usage{
launch(
  script_path,
  rebind = NULL,
  label = NULL,
  external_inputs = NULL,
  force = FALSE,
  duckdb_seed = NULL,
  ...
)
}
\arguments{
\item{script_path}{Either a path to an R script (script mode) or a
braced expression block \code{{ ... }} (inline mode). Dispatch is by
syntax: a literal \code{{ ... }} in the call triggers inline mode;
anything else is resolved as a path.}

\item{rebind}{Optional named list that overrides what each
\code{grab()} inside the script resolves to. List values may be bare
R objects (stowed inline through the normal versioning path) or
reference constructors (\code{\link[=mr_hash]{mr_hash()}}, \code{\link[=mr_run]{mr_run()}}, \code{\link[=mr_variant]{mr_variant()}},
\code{\link[=mr_as_of]{mr_as_of()}}) that resolve to existing versions without
round-tripping through R memory.}

\item{label}{Optional string marking this run as belonging to a tracked
variant (labeled experimental thread). Empty / whitespace-only labels
are rejected; whitespace is trimmed. See \emph{Variants and swappability}
in docs/design.md for the full semantics.}

\item{external_inputs}{Optional named list with fields \code{files} (a
character vector of paths) and/or \code{env} (a character vector of
environment variable names). Each declared input is hashed and
recorded on the run row so later staleness checks can detect
changes. Missing files error \emph{before} the script is sourced.}

\item{force}{Logical, default \code{FALSE}. Reserved for skip-on-fresh control
in a future release; currently accepted and silently ignored.}

\item{duckdb_seed}{Optional numeric seed in \verb{[-1, 1]}. When set,
modelrunnR calls \verb{SELECT setseed(duckdb_seed)} on the DuckDB
connection immediately before evaluating the block, so lazy-tbl
samplers (\code{dplyr::slice_sample()}, \code{RANDOM()}, \verb{USING SAMPLE})
produce reproducible output across runs with the same seed. The
value is stored on the run row. Note: this is DuckDB's RNG, not
R's -- \code{set.seed()} does not reach DuckDB. The RNG state is not
restored after the block.}

\item{...}{Reserved for future arguments and for catching the
removed \code{pin}/\code{data} arguments with a clear error message.}
}
\value{
The run record (one row of \verb{_mr_runs}), invisibly.
}
\description{
\code{launch()} is the tracked-execution entry point. It runs user code
inside an instrumented context that watches for \code{grab()} and
\code{stow()} calls, measures wall-clock duration, and writes a run
record to \verb{_mr_runs} whether the code succeeds or errors.
}
\details{
The code runs in a fresh environment whose parent is \code{globalenv()}.
\code{grab} and \code{stow} are injected directly into that environment so
the tracked code can call them bare without a preceding
\code{library(modelrunnR)}.
}
\section{Script, inline, and relaunch modes}{

\code{launch()} dispatches on the first argument:
\itemize{
\item \strong{Script mode} -- \code{launch("fit.R")} sources a file.
\item \strong{Inline mode} -- \code{launch({ ... })} evaluates a braced block as
tracked code, with no script file on disk. Useful for vignettes,
quick experiments, and one-off tracked runs. The step identifier
is derived from the deparsed expression's hash
(\code{"<inline:<short-hash>>"}), so editing the expression produces a
new tracked step rather than silently comparing against a prior
run's history.
\item \strong{Relaunch mode} -- \code{launch(mr_label("baseline"))} looks up the
most recent run tagged with that label and re-executes its code.
For inline pipelines the stored snapshot on the run row is used
directly. For file pipelines the script file is re-sourced from
disk; if the file is gone, the stored snapshot is used and an
informational message is emitted. The label is auto-inherited
onto the new run unless the caller passes an explicit \code{label}.
}
}

\section{Shadowed \code{source()}}{

During a tracked launch, \code{source()} inside the code (and inside
any transitively-sourced helper) is shadowed with a wrapper that
records each sourced file's path + byte hash on the run row.

The wrapper's default for \code{local} is \code{TRUE} (resolving to the
caller's frame), whereas \code{base::source()}'s default is \code{FALSE}
(which evaluates into \code{globalenv()}). Scripts that rely on
\code{source("helper.R")} populating \code{globalenv()} will instead find
their helpers scoped to the tracked environment. Explicitly
passing \code{source("helper.R", local = FALSE)} still works.
}

