% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prune_variants.R
\name{prune_variants}
\alias{prune_variants}
\title{Delete a labeled variant}
\usage{
prune_variants(script, label, dry_run = FALSE)
}
\arguments{
\item{script}{Path to the script whose variant should be removed.}

\item{label}{The variant label to delete.}

\item{dry_run}{If \code{TRUE}, print the summary without deleting.}
}
\value{
Invisibly, a list with fields \code{script} (normalized path),
\code{label}, \code{n_runs} (rows deleted), and \code{run_ids} (character vector
of deleted run IDs).
}
\description{
Removes all \verb{_mr_runs} rows for \code{script} whose \code{variant_label}
matches \code{label}. Versions the deleted runs produced fall back
under the normal "referenced by recent runs" protection — if a
downstream plain run consumed one of them, it stays; otherwise,
the next \code{prune()} call is free to collect it.
}
\details{
Downstream labeled variants are left alone. Tearing down a whole
labeled pipeline requires calling \code{prune_variants()} at each
level.
}
