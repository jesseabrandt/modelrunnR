% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_con.R
\name{mr_con}
\alias{mr_con}
\title{Return the DuckDB connection modelrunnR is using}
\usage{
mr_con()
}
\value{
A \code{DBIConnection} (specifically, a \code{duckdb_connection}).
}
\description{
Exposes the package's persistent DuckDB handle so callers can drop
to raw SQL for workflows \code{dbplyr} doesn't express cleanly — custom
sampling schemes, manual table inspection, etc.
}
\details{
The returned connection is the same one \code{\link[=grab]{grab()}} and \code{\link[=stow]{stow()}} use
internally. Do not call \code{DBI::dbDisconnect()} on it; \code{modelrunnR}
manages its lifecycle.
}
\examples{
\dontrun{
con <- mr_con()
DBI::dbGetQuery(con, "SELECT COUNT(*) FROM my_table")
}
}
