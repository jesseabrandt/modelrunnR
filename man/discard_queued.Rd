% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discard_queued.R
\name{discard_queued}
\alias{discard_queued}
\title{Discard queued runs}
\usage{
discard_queued(variant_label = NULL, before = NULL, dry_run = FALSE)
}
\arguments{
\item{variant_label}{Optional character; restrict to rows with this
\code{variant_label}. Pass \code{NA} to restrict to rows with no label.}

\item{before}{Optional \code{POSIXct} (or anything \code{as.POSIXct}-coercible);
restrict to rows where \code{started_at < before}.}

\item{dry_run}{Logical; if \code{TRUE}, report what would be deleted but
do not delete. Default \code{FALSE}.}
}
\value{
Invisibly, a list with \code{n_runs} (rows that were or would be
deleted) and \code{run_ids} (their ids).
}
\description{
Deletes rows from \verb{_mr_runs} whose status is \code{"queued"}, optionally
filtered by variant label or start time. Useful when re-rendering a
qmd has accumulated stale \code{queue()} calls that no longer reflect the
source.
}
\details{
Only \code{queued} rows are touched: completed (\code{success},
\code{skipped_fresh}), failed (\code{error}), and \code{interactive} rows are left
alone. Use \code{prune()} / \code{prune_variants()} for those.
}
