% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_binds.R
\name{mr_binds}
\alias{mr_binds}
\title{Build a batch of \code{launch(rebind = ...)} envelopes}
\usage{
mr_binds(..., mode = c("zip", "cross"), .labels = NULL)
}
\arguments{
\item{...}{Named sweep arguments. Each value is a vector / list of
per-envelope values for that rebind slot.}

\item{mode}{\code{"zip"} (element-wise) or \code{"cross"} (Cartesian product).}

\item{.labels}{Optional character vector of explicit labels (one per
envelope after expansion).}
}
\value{
An \code{mr_binds} object: a classed list of envelope lists, each
suitable as the \verb{rebind =} value of a single \code{launch()}.
}
\description{
Pure list constructor for a sweep of rebinds. Each named \code{...}
argument is the vector of values for that rebind slot. Pass the
returned object to \code{launch(rebind = ...)} and the launch fans out
into one run per envelope.
}
\details{
Two expansion modes:
\itemize{
\item \code{mode = "zip"} (default) -- element-wise pairing. All \code{...}
arguments must share one length N (length-1 recycles to N).
\item \code{mode = "cross"} -- Cartesian product. N = product of lengths.
}

Values flow through unchanged: pass \code{mr_variant("clean")} to
resolve to a labeled variant's latest hash, \code{mr_hash("...")} to
address a specific version, or a bare R value to stow inline. If
you want to sweep over variant labels, use \code{mr_variants(...)} to
avoid passing bare strings as literal values.

Optional \code{.labels} is a character vector (length N after expansion)
of explicit labels for the runs. When \code{NULL} (default), labels are
left unset and the existing label-auto-propagation path fills them
from upstream variants.
}
\seealso{
\code{\link[=mr_variants]{mr_variants()}} for sweeping over variant labels;
\code{\link[=mr_envelopes]{mr_envelopes()}} for hand-built envelopes when \code{mr_binds()}'s
sweep API isn't expressive enough.
}
