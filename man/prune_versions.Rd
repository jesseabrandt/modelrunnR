% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prune_versions.R
\name{prune_versions}
\alias{prune_versions}
\title{Explicit garbage collection for stored versions}
\usage{
prune_versions(
  name = NULL,
  keep = NULL,
  keep_latest = FALSE,
  older_than = NULL,
  force = FALSE
)
}
\arguments{
\item{name}{Optional logical name to restrict pruning to.}

\item{keep}{Optional integer. Keep the N most recent versions
(by \code{first_seen}) per logical name.}

\item{keep_latest}{Logical. If \code{TRUE}, keep only the single
newest version per logical name.}

\item{older_than}{Optional character; strings like \code{"30d"},
\code{"6h"}, \code{"15m"}, or \code{"45s"}. Prunes versions whose \code{first_seen}
is older than the parsed duration.}

\item{force}{Logical. If \code{TRUE}, also prune versions referenced
by run history (destructive -- breaks \code{grab(from_run = ...)} for
the removed versions).}
}
\value{
A data frame of the versions that were actually pruned,
invisibly.
}
\description{
Removes stored versions according to a policy. Without
\code{force = TRUE}, versions referenced by any \verb{_mr_runs.outputs}
row are protected so that \code{grab(..., from_run = ...)} keeps
working for existing run history.
}
\details{
Policy arguments combine: \code{keep_latest} is applied first, then
\code{keep = N}, then \code{older_than}. With \code{name = NULL} the policy
applies to all stored logical names.
}
