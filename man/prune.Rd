% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prune.R
\name{prune}
\alias{prune}
\title{Prune stored values}
\usage{
prune(
  name = NULL,
  by = c("auto", "version", "run", "age"),
  run_id = NULL,
  keep = NULL,
  keep_latest = FALSE,
  older_than = NULL,
  force = FALSE
)
}
\arguments{
\item{name}{Optional logical name to restrict pruning to.}

\item{by}{One of \code{"auto"}, \code{"version"}, \code{"run"}, \code{"age"}. The default
\code{"auto"} dispatches on shape. \code{"version"} requires a Shape A name;
\code{"run"} requires a Shape B name; \code{"age"} is a shape-agnostic
shortcut that uses only \code{older_than}.}

\item{run_id}{Character vector of run ids to prune (Shape B only).}

\item{keep}{Integer; keep the N most recent versions (Shape A) or
runs (Shape B). Applied per logical name.}

\item{keep_latest}{Logical; shorthand for \code{keep = 1}. Shape A only.}

\item{older_than}{Duration string (\code{"30d"}, \code{"6h"}, \code{"15m"},
\code{"45s"}). Works on both shapes.}

\item{force}{Logical. If \code{TRUE}, overrides protection (run-referenced
versions on Shape A, variant-labeled runs on Shape B).}
}
\value{
Invisibly. For a single-shape call, a data frame describing
what was pruned. For calls that span both shapes (\code{name = NULL}
with \code{by = "auto"}, or \code{by = "age"}), a list with \verb{$versioned} and
\verb{$append} data frames.
}
\description{
Remove stored data from the modelrunnR store. Works on both storage
shapes:
}
\details{
\itemize{
\item \strong{Versioned (Shape A)} — drops entire \verb{(name, content_hash)} rows
from \verb{_mr_versions} and their physical artifacts.
\item \strong{Append (Shape B)} — deletes rows from the growing append table
by \code{run_id} / age, keeping the registry row so the accumulator
still exists under its logical name.
}

Dispatches on the shape of \code{name}. Without \code{name}, applies the
policy to every logical name in the store (both shapes).
}
