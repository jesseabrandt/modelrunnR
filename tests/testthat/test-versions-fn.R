# All Shape-A data-frame versioning tests (versions() on stowed data frames)
# were deleted: Shape B (append-mode) does not use _mr_versions for data frames.
# versions() / versions(name) still apply to artifacts (Shape A non-tabular
# values). Coverage for artifacts is in test-artifacts.R.
